# AGENTS.md

## 项目信息

- 项目英文名：Live Streaming Slicing Workflow
- 项目中文说明：直播切片工作流
- 项目目录：`C:\Users\10578\Documents\New project 2`
- 当前目标：Windows 本地后台处理链路 MVP

## 协作方式

用户是编程新手，不懂代码和终端命令。每次执行任务时必须用中文、保姆级说明，并严格按阶段推进：

1. 先说明准备做什么。
2. 再执行检查或修改。
3. 修改后说明改了哪些文件。
4. 告诉用户如何测试。
5. 最后更新项目文档，记录当前进度和下一步。

## 技术栈

- 后端：Python + FastAPI
- 前端：HTML + CSS + JavaScript + Jinja2
- 数据库：SQLite
- 视频处理：FFmpeg / FFprobe
- AI 分析：预留 OpenAI-compatible API 或本地大模型接口
- 语音转写：预留服务接口

## 当前 MVP 范围

- 新建直播视频处理任务。
- 上传本地视频或选择 NAS / 本地目录中的已有视频。
- 每条视频生成独立任务目录。
- 提取音频。
- 转写为带时间戳文本。
- 生成分钟级 / 分段级时间线文本。
- AI 分析候选短视频片段。
- 后台查看并编辑候选片段。
- 自动切割视频并输出短视频文件。
- 展示任务状态、处理进度和异常信息。

## 当前不做

- MacBook 自动录屏。
- 自动识别抖音 / B站直播间并检测是否开播。
- 自动发布到抖音 / B站。
- 自动生成封面。
- 多用户权限系统。

这些能力只在架构上预留接口，不要擅自扩大首版范围。

## 修改规则

- 修改前先检查当前目录、Git 状态和相关文件。
- 不要随意删除旧文件。
- 不要覆盖用户已有内容，除非用户明确同意。
- 不要擅自切换到 React / Vue。
- 修改后必须更新 `DEVELOPMENT_LOG.md` 和 `NEXT_STEPS.md`。
- 如果新增或调整页面结构，也要同步更新 `docs/UI_REFERENCE.md`。

## UI 参考

后续前端页面实现必须优先参考：

```text
docs/design/live_streaming_slicing_workflow_ui_16x9.png
```

设计方向：Apple 风格、简洁、高级、留白充足、轻量玻璃拟态、卡片式布局、蓝色作为主强调色，适合作为个人本地工作流后台。

# Next Steps

## 2026-08-24 稳定 V1 整改路线

- [x] P0.1：Pytest 与活动数据库、媒体目录彻底隔离，危险整表清理增加 fail-closed 校验。
- [x] P0.2：修复活动库 17 条孤儿外键；以 8 个不可见 tombstone 保留发布、字幕和人工复核历史，最终 `foreign_key_check=0`。
- [x] P0.3：永久删除改为“同卷隔离暂存 → 数据库提交 → 最终清理”，提交失败可恢复，最终清理失败可重试。
- [x] P0.4：SQLite Online Backup 固定为不依赖 WAL/SHM 的单文件快照。
- [x] P1.1：收紧媒体读取与任务目录边界，补齐核心读写路径的 traversal / arbitrary-file 回归测试；并补充同名目录原子预占和进程树退出确认。
- [x] P1.2a：Workflow Job 增加每次 claim 唯一的 `lease_token`，旧 Worker 的心跳、进度、checkpoint、终态和 release 均不能覆盖新执行。
- [x] P1.2b：Publish Job 使用现有 `execution_id` 完成所有写回 fencing，并让 Windows Publish Worker 对重复 execution 幂等；跨进程锁、恢复 fail-closed 和 Scheduler 优雅停机已覆盖。
- [x] P1.3a：任务状态接口受控转换；Task/Job lease 代际一致；自动流水线取消恢复、READY 终态和关联发布清理明确；切片批次编号、输出目录、Task 终态和数据库激活原子化。
- [ ] P1.3b：为自动流水线增加按步骤持久化 checkpoint 和副作用复用，补齐进程重启后的精确续跑；再处理字幕批准批次原子性与中断目录清理。
- [ ] P1.3c：修复数据库唯一索引重建失败被静默吞掉的问题，并为幂等迁移增加可追踪版本与失败证据；不在本轮直接迁移活动库。
- [ ] P1.4：统一第三方 AI/FFmpeg 超时、错误 JSON、429/5xx 与重试幂等边界，并避免重复计费。
- [ ] P1.5：在不扩大个人本地项目范围的前提下处理密钥日志、输入校验和本地管理员接口门禁。
- [ ] P2：拆分 God Service、去除查询重复、补核心集成/故障测试和可观测性；不做全面重构。

### 本轮人工检查

1. 正常打开 `http://127.0.0.1:8001/` 和发送中心，确认页面可用、Scheduler 正常、Windows Worker 正常。
2. 不需要点击“立即发送”；本轮没有执行真实投稿，也没有改变 28 条 `NEED_REVIEW` 的人工确认边界。
3. 若未来永久删除返回 `cleanup_pending`，不要手工移动隔离目录；保留返回信息和 manifest，使用后续安全清理入口重试。
4. 修复前数据库备份位于 `data/backups/workflow-before-foreign-key-repair-20260824-130321-535723-35a0f972.sqlite3`，只有活动库无法通过完整性检查时才考虑恢复，不要直接覆盖当前数据库。
5. P1B.1/P1B.2 与 P1.3a 代码、隔离测试均已完成；活动库尚未增加 `workflow_jobs.lease_token`，本轮也未重启正式服务。下一独立轮先确认没有活动 Workflow Job，再做在线备份、幂等迁移和正式重启烟测。
6. P1.3a 不需要手工修改数据库。合并后可用一条低风险测试任务验证：取消后显示“已取消”；正常完成后停在“待人工确认发布”；不要点击真实发布。

## 2026-08-24 工程体检确认的原始整改顺序

本轮审计已经完成，用户已确认按以下顺序逐轮整改；不要一次性全面重构：

1. **P0.1 测试数据库保护**：让 pytest 只能使用唯一临时库，发现活动库路径立即拒绝启动。
2. **P0.2 数据一致性**：先做 WAL-aware 备份和 dry-run，再逐条处理活动库 17 条外键违规；修复前不要手工删记录。
3. **P0.3 可恢复删除**：把“直接删文件后提交数据库”改成带隔离区和 manifest 的两阶段删除。
4. **P1.1 Secret 与本地鉴权**：读取接口不再返回原始 API Key/OAuth Token；非 loopback 部署要求明确保护（尚未开始）。
5. **P1.2 路径边界**：任务目录、媒体响应、Worker execution/account id、浏览器 profile 和手工发布包边界均已完成。
6. **P1.3 Job/Publish fencing**：Workflow lease token 与 Publish execution fencing 已完成；旧 Worker/旧 execution 不能覆盖新 attempt，重复 execution id 不会再次进入 Publisher。
7. **P1.4 状态与部分成功**：逐步封住任务状态跳跃、切片/字幕批次半提交、转写旧结果复用和 AI partial 成本边界。
8. P0/P1 稳定后，再补 Coverage、真实故障测试、readiness/日志，然后渐进拆分 `publish_service.py` 与前端大脚本。

整改期间继续遵守：每轮范围有限、独立测试、独立提交、可回滚；不更换 SQLite、不引入微服务/React/Vue、不删除历史 Publisher、不改掉 `NEED_REVIEW` 人工复核。

## 2026-08-23 长直播四阶段进度

- [x] PR 1：模式必选、已有文件入口、媒体/磁盘预检、持久化重型 Job、转写断点与词级时间戳。
- [x] PR 2：`long_live_talk` 5 分钟重叠窗口、每小时覆盖、全局配额、去重、90% 覆盖门禁和窗口级恢复。
- [x] PR 3：统一字幕 track/revision/cue、pysubs2 导入导出、wavesurfer 波形与专业编辑器。
- [x] PR 4：字幕审核暂停、AI 建议 revision、异步批量烧录、NVENC 回退和发送中心门禁。

当前验证重点：四个开发阶段均已落地。合并时必须按 PR 1 → PR 2 → PR 3 → PR 4 的顺序；每个 PR 只合并到它标明的前序分支，最终再进入 `master`，不要跳过中间数据库与接口变更。

### PR 4 人工检查

1. 新建一个低风险全自动测试任务并完成切片；任务应停在“字幕待审核”，发送中心不能提前出现该任务。
2. 打开字幕工作台，修改并审核字幕后点击“审核并批量烧录”；观察队列进度，完成后确认带字幕视频可播放，任务随后恢复生成文案。
3. 另建测试任务，在暂停页明确点击“跳过字幕，使用原片继续”；发送中心应显示原片来源，不应误用旧字幕文件。
4. 选中少量 cue 请求 AI 建议时会调用已配置的文本 AI 并产生额度；先检查 Provider，再确认差异只包含文字，人工接受前 active revision 不变。
5. 取消一条正在烧录的测试任务，确认状态进入已取消、旧 active 字幕仍可播放、刷新后可以重试。不要用真实待发布内容做这一项。

## 2026-08-23 v2.1.0 主线同步后检查

1. 最终提交进入 `master` 后先查看 GitHub CI；只有 Python、Windows host smoke 和 Docker image smoke 全部通过，才把该提交作为 2.1.0 候选版本。
2. 在片段审核页测试“全选当前列表 / 取消全选”，确认启用数量和半选状态正确；点击“生成切片”后应看到排队与运行进度，连续点击不会创建两条切片任务。
3. 在任务详情启动一次低风险 AI 分析，确认分析期间 Prompt、候选数量和三个分析入口都锁定，完成或失败后恢复；这项测试会调用所选 AI，请自行确认额度后再操作。
4. 本次主线同步不创建 `v2.1.0` Tag 或 GitHub Release。发布前必须在最终 `master` 上重新运行 Windows 10/11 + Docker Desktop 实机验收和 `release_gate.ps1`，旧 2.0.0 报告不能作为 2.1.0 证据。
5. 旧 `fix/task-automation-button-experience` worktree 的未提交内容继续原样保留；不要直接整批合并，其中“仅隐藏任务”会与当前永久删除托管产物的语义冲突。

## 2026-08-22 整合后使用与合并检查

1. 当前 `feature/task-defaults-and-safe-startup` 已同时包含 PR #38 原生运行能力和发送中心新版；推送后先检查 PR #38 的 GitHub CI 与差异，再由用户决定是否合并到 `master`，不要自动删除分支。
2. 日常排期时，在对应“原始处理任务”点击“全选本任务”，确认底部已选数量后点击“设置排期”，先查看“预览排期”，确认时间无误后再保存；该全选不会跨任务组。
3. 本次实机验收只打开了排期抽屉，没有保存排期或触发真实投稿；既有 4 条排期保持不变。真实发布仍需人工确认账号登录、验证码和平台风控。
4. 如果需要核对旧草稿升级，可保留 `data/backups/workflow-before-publish-migration-20260822-121307-772508-140328-70deb9bc.sqlite3`；不要手工删除数据库、B站历史或浏览器 Profile。

## 2026-08-22 发送中心抖音规则检查方法

1. 合并并启动新版本后打开 `http://127.0.0.1:8001/publish?platform=bilibili`，页面仍应固定显示抖音，不能出现 B站切换卡；既有 B站数据库记录不会被删除。
2. 若当前只有一个状态正常的抖音账号，内容卡不应再显示“缺少：发布账号”，账号下拉框应自动选中该账号；多个账号需要手动选择，登录失效应显示“账号需登录”。
3. 在任一“原始处理任务”点击“全选本任务”，只应勾选该任务组的视频；取消一条后复选框显示部分选择，再次全选和“取消全选”都应生效，折叠后仍可操作。
4. 检查标题旁 `当前字数/30`、标签数量和简介 `当前字数/35`；手工输入超过 30 字标题、少于 15 字简介或非 4～6 个/非 2～3 字标签时，保存应直接提示原因，不会静默截断。
5. 首次打开升级后的页面会自动升级未排期抖音旧草稿并在写入前备份 SQLite；已排期、历史、B站、已移除和失效切片不变。若 AI 失败，原标题、简介和标签保持不变，可稍后点击“AI 重写文案”手动重试。
6. 本次验收不需要点击“立即发送”。真实灰度仍必须使用低风险素材，并由用户在独立 Chrome 中人工完成登录、验证码和风控确认。

## 2026-08-21 原生日常运行与 Docker 回退

1. 当前工作台由 Alter 中的 `Niuma-Studio` 运行；访问 `http://127.0.0.1:8001/`。发送中心应显示 Windows Worker 正常。
2. 不使用 Alter 时，可在项目目录运行 `.\scripts\start_native.ps1`；只查看工作台可加 `-SkipWorker`。对应停止命令为 `.\scripts\stop_native.ps1`。
3. `NiuMa Studio Docker Watcher` 计划任务当前只是禁用，没有删除。日常原生模式不要启用它，否则登录时可能重新拉起旧 Docker 流程。
4. 回滚前先停止 Alter 中的 `Niuma-Studio` 和本项目 Worker，再运行 `docker compose up -d`；确认容器健康后才重新启用 watcher。禁止使用 `docker compose down --volumes`、`docker volume prune` 或删除 `data`、E 盘任务目录。
5. 切换前数据库备份是 `data/backups/workflow-pre-native-cutover-20260821-150620.sqlite3`，完整性为 `ok`。只有原生与 Docker 都无法读取正式库时才考虑恢复，不要覆盖当前数据库。
6. 本批原生启动脚本专项测试 `11 passed`，PowerShell 5.1/7 解析均为 0；隔离与正式环境核心页面均为 HTTP 200，未触发真实投稿。

## 2026-08-16 任务名称历史候选检查方法

1. 重启 NiuMa Studio 后打开“新建任务”，点击“任务名称”输入框；应出现应用控制的下拉候选列表，第一条是最近创建且仍然可见的任务名称，较旧任务依次向下排列。
2. 已隐藏或永久删除的任务名称不应出现在候选里；点击任意候选可完整填入，也可以继续手动输入任意新标题。
3. 新建一条任务后，下次打开“新建任务”页面，该任务名称应自动成为候选第一项。
4. 本次只读展示候选，不会修改现有任务、数据库、媒体处理或发布流程；真实创建任务前仍按原方式填写或选择任务名称。

## 2026-08-13 GitHub 主页任务详情与发送中心图片检查方法

1. 打开本次 PR 的 `README.md` 预览，滚动到“产品预览”，确认“任务详情”显示任务详情图、“发送中心”显示发送中心图，且两张画面完整、位置正确。
2. 打开 `README.en.md` 预览，确认“Task detail”和“Publishing center”与中文主页分别使用相同的对应图片。
3. 点击两张图片查看原图，确认资源均为 `2560 × 1271` 的 PNG；GitHub 页面可能会按浏览器宽度缩放显示，但仓库中的原图没有压缩或降采样。
4. 本次只替换 GitHub 主页图片，不需要启动服务，也不会影响本机素材、SQLite、排期或真实投稿。

## 2026-08-03 DeepSeek Flash 切换后检查方法

1. 重启 NiuMa Studio，使本机 `.env` 的新模型配置重新加载；不需要重新填写或更换 DeepSeek API Key。
2. 打开系统状态页，确认“文字稿分析”和“发送中心发布文案”的模型都显示 `deepseek-v4-flash`。
3. 用一条测试任务运行“远程 AI 分析”，再到发送中心执行一次“AI 补齐标题/话题/简介”；两处运行记录都应显示 `deepseek-v4-flash`。
4. 本次只切换模型，不会自动发稿，也不会改动本地 Ollama、火山引擎转写、账号登录或平台风控配置。
5. 本次专项测试与两类 DeepSeek 真实连通测试已通过；创建 PR 前需另行排查 `tests/test_publish_center_browser.py` 的历史记录等待超时，再重新运行完整测试。

## 2026-08-03 v2.0.0 发布前最终门禁

1. 先合并 Windows 实机验收兼容修复 PR，再把本地 `master` 快进到云端最新提交；PR 未合并前不要创建 `v2.0.0` Tag 或 Release。
2. 验收前临时停止 `NiuMa Studio Docker Watcher` 和正式容器，确认 8001 空闲；验收与门禁完成后恢复原有监控任务，不删除计划任务、Cookie 或浏览器 Profile。
3. 在 Windows 10/11 + Docker Desktop Linux Containers 上依次运行 `pre_upgrade.ps1`、`acceptance.ps1`、`release_gate.ps1`；三步全部通过后才进入人工发布确认。
4. `acceptance-results/`、`backups/`、`.env`、SQLite、视频、Cookie、浏览器 Profile 和本地日志只保留在本机，不提交或上传到 GitHub。
5. 如果 Docker Hub 或 Debian 官方 CDN 在本机不可达，只通过当前 PowerShell 会话的构建参数切换镜像；仍需运行不带 `-NoBuild` 的完整验收，不允许伪造报告或跳过正式数据指纹。

## v2.0.0 发布后优先验收

1. 打开 Docker Desktop 并运行 `niuma-studio`，浏览器访问 `http://127.0.0.1:8001`；确认左侧显示“v2.0 本地高光生产版”，系统状态与发送中心显示 Windows Worker 正常。
2. 用一条短测试视频开启全自动任务，确认任务详情的 10 步进度、日志、候选数和输出数每 3 秒局部更新；流程完成后能进入片段审核和发送中心。
3. 在片段审核修改一条候选的启用状态或时间，点击“保存并同步发送中心”；确认必要时生成新切片，发送中心只使用当前激活版本，旧执行证据仍留在历史。
4. 在内容准备核对抖音 / B站的视频、标题、简介、话题、封面、账号和可见范围；设置一个跨午夜测试排期，确认月历、日期详情和任务清单按北京时间升序。
5. 抖音和 B站分别只选一条低风险测试内容做真实灰度；必须在 Chrome 独立窗口人工完成登录与平台验证。遇到验证码、风控或结果不确定时先核对创作者中心，不要直接重试。
6. 连续运行一段时间后检查 Scheduler、Docker Watcher、Windows Worker 和发布事件日志，并验证 SQLite 备份与 E 盘剩余空间。

## 2026-08-03 排期日历展开与排序检查方法

1. 打开 `http://127.0.0.1:8001/publish`，按 `Ctrl + F5` 强制刷新，然后进入“排期计划”。
2. 同一天有 3 条以上排期时，月历仍会预览前两条并显示“另有 N 条”；点击日期格后，月历下方应展开当天全部排期，时间从早到晚排列。
3. 当天详情会显示任务标题、账号和状态；点击某一条只会滚动并蓝色高亮下方原任务，不会改变排期或立即发送。
4. 下方任务清单应先列出全部已排期任务，并按北京时间从最早到最晚排列；未排期任务统一放在已排期任务后面。
5. 调整、清除或取消排期后，月历、当天详情和主清单应立即更新。切换抖音/B站或月份时，之前展开的日期会关闭，两个平台不会混排。
6. 日期格也可以按 `Tab` 聚焦后使用 Enter 或空格打开。验证本功能不需要点击“立即发送”，请勿改动即将真实投稿的排期。

## 2026-08-02 续接最晚排期使用与检查

1. 打开 `http://127.0.0.1:8001/publish` 并按 `Ctrl + F5`，进入“内容准备”或“排期计划”，勾选同一平台需要安排的内容后点击“设置排期”。
2. 抽屉中的每日开始、结束时间应默认显示 `07:00` 和 `00:00`；这是新默认值，不会改动以前已经保存的排期，也可以为本次排期手动修改。
3. 先选择发布间隔，再点击“接在当前平台最晚排期后”。页面应显示“当前最晚”和“本次第 1 条”时间；例如当前最晚 19:00、间隔 3 小时，本次应为 22:00。
4. 如果加上间隔后落在 00:00 至 07:00 的不可发布区间，系统会自动顺延到 07:00；正好得到 00:00 时仍允许排期。
5. 当前平台没有其他未来排期时，页面会提示手动选择，不会覆盖第 1 条时间。调整时间、间隔或每日窗口后，必须重新点击“预览排期”再确认应用。
6. 抖音和 B站分别续排；切换平台后需重新勾选。验证此功能只需要预览，不必点击立即发送，也不会绕过账号登录、验证码或平台风控。

## 2026-08-02 片段审核同步发送中心检查方法

1. 打开目标任务的“片段审核”，勾选想发布的候选片段；无需再依次点击“保存修改”“生成切片”和旧同步按钮，直接点击“保存并同步发送中心”。
2. 页面会先保存当前勾选、标题、摘要和出入点；如果启用选择与最新成片不一致，会显示“正在保存并生成”，完成后自动打开发送中心并展开当前任务。
3. 当前真实任务 `6753ce9f3bfd` 已同步成功，发送中心抖音“内容准备”应显示 3 条：大田怕鸟、渣渣爷爷名字、Circus 跳舞；每条均为待准备状态，可以分别点击“加入发布计划”。
4. 如果当前审核内容和成片完全一致，重复点击只会执行幂等同步，不会重复生成；如果修改了启用状态、标题、摘要、出入点，或成片文件缺失，系统才会创建安全的新切片版本。
5. 新同步内容不会自动排期或投稿。加入发布计划前仍需逐条核对账号、标题、简介、话题、封面、可见范围和具体北京时间；二维码、验证码、滑块与平台风控继续由人工处理。

## 2026-08-02 内容准备已排期标记检查方法

1. 打开 `http://127.0.0.1:8001/publish`，按 `Ctrl + F5` 强制刷新，然后停留在“内容准备”。
2. 已排期卡片右上角应显示蓝色“已排期 + 具体北京时间”，整张卡片同时出现浅蓝底、蓝色边框和左侧强调线，底部按钮显示“调整排期”。
3. 未排期卡片不显示该标签，仍保持普通白色卡片和“加入发布计划”按钮；这两种状态应能快速区分。
4. 新增、调整或清除排期后，卡片标记应在当前页面直接更新，不需要手动刷新。验证时只操作测试任务，不要改动即将真实发送的排期。
5. 本次只优化状态提示，不改变调度器、账号、素材或真实投稿流程；真实投稿前仍需逐条核对账号、文案、封面、可见范围和排期。

## 2026-08-02 发送中心封面计数修复后的检查方法

1. 打开 `http://127.0.0.1:8001/publish` 并刷新页面；当前抖音没有缺失封面时，应显示禁用的“补充抖音缺失封面”，不再出现错误的“（8）”。
2. 切换到 B站后，按钮会按 B站自身数据重新显示缺失数量；现场数据目前为 8 条，这 8 条确实没有记录封面路径，并非抖音任务缺失。
3. 只有确认当前平台确实需要自动补封面时才点击按钮。点击后只会处理当前平台，不会跨平台修改另一侧任务，也不会自动排期或投稿。
4. 本次修复只校正统计和批量处理边界，没有替 B站自动生成这 8 张封面；如需补齐，可先在 B站内容准备区核对对应任务，再手动点击 B站封面按钮。
5. 真实投稿前仍需逐条核对账号、标题、简介、封面、可见范围和排期；二维码、验证码、平台风控及人工确认边界保持不变。

## 2026-08-01 康熙笑点优先 V2 使用与验收

1. 新建康熙任务时，把“选片模式”设为“综艺笑点优先”，候选池保持 12 条、最终启用目标保持 5 条；质量不足时页面出现少于 5 条属于正常结果。
2. 旧任务可以在任务详情的 AI 分析区切换模式并保存。现有自定义 Prompt 会继续保留；如需使用内置规则，可选择 4 号“康熙笑点优先 V2”。
3. AI 分析完成后，在片段审核页优先检查：是否为 A 级、是否 60–150 秒、是否明确包含铺垫/笑点或反转/补刀或现场反应/收尾，以及是否与相邻候选重复。
4. B 级候选默认关闭，只供人工复核；C 级不会显示。认为 AI 判断不合口味时，点击“不好笑、太碎、铺垫不足、重复、拖沓”之一；确实值得发布则点击“值得发”。
5. 第一次真实素材验收建议直接调用 AI 分析而不要启动全自动发布：分别记录旧版 12 条与 V2 候选的数量、时长、重复话题和至少 4 条“值得发布”人工判断。确认后再生成切片。
6. 音频证据是轻量代理信号；音频不存在或 FFmpeg 读取失败时会显示降级原因，但文字评审仍会继续。它不代表精确笑声识别或说话人识别。
7. 本轮测试没有调用抖音/B站投稿。真实发布前仍需人工核对账号、标题、封面、可见范围和排期，验证码与平台风控继续由人工处理。

## 2026-07-29 任务与发送中心关联后的检查方法

1. 打开任务列表，目标任务的“后续工作流”应显示 `已关联 24/24`；“字幕”会进入该任务专属地址，“发送中心”会进入该任务的内容准备分组。
2. 当前任务 `b84227be2b01` 已经同步完成：12 条新切片各有抖音和 B站内容，全部为 `WAITING` 且没有排期，不会因为本次同步自动投稿。
3. 重新切片后如果页面显示“待同步”，点击“同步发送中心”即可补齐；重复点击不会创建重复任务。
4. 如果某条内容曾由用户主动“移出内容准备”，全局补充仍尊重移出标记；只有任务页或字幕页的明确任务级同步会恢复它，恢复后需要重新确认账号和排期。
5. 字幕完成后从字幕工作台同步，会把尚未排期的内容改为带字幕视频；已经排期的内容必须先取消发送并返回内容准备，系统不会静默替换视频。
6. 发送中心内容准备和排期计划只展示当前激活切片；旧版取消、已发布、失败和待复核证据继续在执行记录中保留。
7. 真实投稿前仍要逐条确认平台、账号、可见范围、标题、简介、封面和排期；二维码、短信、验证码、滑块和风控继续由人工处理。

## 2026-07-29 排期故障恢复后的检查

1. 首条已确认投稿成功的 7 月 28 日 09:00 任务已经补记为 `PUBLISHED`，不得再次点击立即发送；平台未返回视频 ID 或链接不代表可以重发。
2. 剩余 18 条排期已按原顺序移动到北京时间每日 `09:00 / 11:00 / 13:00 / 15:00 / 17:00 / 19:00 / 21:00`，实际范围为 7 月 30 日 09:00 至 8 月 1 日 15:00；每条事件均保留原时间、新时间和故障恢复原因。
3. 打开 `http://127.0.0.1:8001/publish`，健康卡应显示调度器“正常”、Windows Worker“正常”；如果出现“异常重试中”，等待下一轮自动恢复并把安全化原因交给开发助手检查。
4. 7 月 30 日 09:00 首个恢复时段后，只接受 `PUBLISHED`、`NEED_REVIEW` 或明确 `FAILED`；如果仍为 `PUBLISHING`，先查 Worker 执行日志，禁止直接重复投稿。
5. 二维码、短信、验证码、滑块、平台风控和人工确认仍必须人工处理，本次修复不会绕过这些边界。
6. 当前工作区含大量既有未提交修改，本次不会执行 `git add .`、提交、推送或创建 PR；整理工作区后再把本次修复单独提交。

## SQLite 备份维护

1. 已完成首次清理并释放 121.601 GiB；接下来连续观察至少 14 天，确认 `data/backups` 每天最多保留一份有效迁移快照，目录体积不再异常增长。
2. 需要人工复核时，在项目根目录执行 `.venv\Scripts\python.exe scripts\cleanup_database_backups.py`；该命令只预演，不会删除文件。
3. 只有确认预演清单后才使用 `.venv\Scripts\python.exe scripts\cleanup_database_backups.py --apply`；脚本不会触碰 `data/workflow.sqlite3`、任务素材或其他文件。
4. 当前工作区还有发布功能的既有未提交修改；待这些修改整理完成后，再单独提交本次备份防护代码并创建 PR。

## 2026-07-28 执行记录日历与安全删除检查方法

1. 打开 `http://127.0.0.1:8001/publish`，按 `Ctrl + F5` 后进入“执行记录”；顶部应出现当前平台的 42 格执行月历。
2. 有排期的任务按计划日期归档；立即发送但没有排期的旧记录按开始时间归档。点击某一天后，下方列表只显示当天记录，点击“查看全部日期”恢复完整列表。
3. 失败记录应显示“立即发送”和可见范围，不再显示“重新加入内容准备”。只有先前从内容准备主动移出的记录才有“重新加入内容准备”。
4. “立即发送”会保留原失败记录并创建新的调度任务；如果账号未登录、视频文件缺失或 Worker 未连接，页面会先提示修复，不会直接投稿。
5. 已发布、失败、已导出和已取消记录可以单条或批量“删除记录”。确认框会说明：只从正常列表和日历隐藏，不删除视频、执行明细、数据库历史或平台作品。
6. 点击“已删除记录”可查看并恢复安全删除的记录；正在发送、待执行和需人工复核记录不能删除。
7. 真实投稿仍需人工处理二维码、短信、验证码、滑块和风控；验证日历或删除功能时不需要点击真实发送。
8. 本次出现 500 的任务已经成功创建过一条重试记录，并因“引战夸张”风险标记进入“需人工复核”；不要再次重发，先打开创作者中心确认平台没有作品，再按实际结果选择“标记已发布”或“标记失败”。

## 2026-07-23 Docker 自动联动后的日常检查

1. 日常只需打开 Docker Desktop 并运行 `niuma-studio`，不再输入 Worker 或一键启动命令。
2. 打开 `http://127.0.0.1:8001/publish`，等待“Windows Worker”显示“正常”；刚启动时可点击“重新检测”。
3. 在 Docker Desktop 中停止项目后，Worker 会在 15 秒后关闭；重新运行项目会再次自动启动。
4. 如果持续未连接，先在 Docker Desktop 中停止并重新运行本项目；仍未恢复时交给开发助手检查观察器和 Worker 日志。
5. 真实投稿仍必须人工完成登录、二维码、短信、验证码和风控确认；自动联动只负责准备发送环境，不会绕过平台验证。

## 2026-07-22 任务归类与安全移出后的检查方法

1. 打开 `http://127.0.0.1:8001/publish` 并按 `Ctrl + F5`，确认“内容准备”最外层按任务名称分组，组头能看到原视频文件名、创建时间和当前平台待准备数量。
2. 最新任务应默认展开，其余任务默认折叠；点击“展开 / 收起”只改变当前任务组，不影响标题、简介、账号或排期数据。
3. 分别切换抖音和 B站，确认分组数量会跟随当前平台变化；没有当前平台内容的任务组应隐藏，切换平台仍会清空已勾选任务。
4. 如需验证移出功能，只选择一条确认不再发送的测试内容，点击“移出内容准备”并阅读确认提示；原视频、裁剪文件、字幕和另一个平台内容都应保留。
5. 被移出的记录应进入“执行记录”并显示“重新加入内容准备”；恢复后状态为等待处理，不会自动恢复原排期或触发真实投稿。
6. 点击“补充缺失任务”或重跑全自动流程时，主动移除的当前平台内容不应重新出现；如需重新使用，必须从执行记录明确恢复。

## 2026-07-19 发送中心旧入口清理后的检查方法

1. 打开 `http://127.0.0.1:8001/publish` 并按 `Ctrl + F5`，在“内容准备”勾选一条任务；底部批量栏不再出现“立即发送”。
2. 底部批量栏仍应显示当前平台、账号选择、“批量设置当前平台账号”“批量 AI 补齐”“设置排期”和“取消选择”。
3. 点击“设置排期”，确认仍能预览并保存北京时间排期；这一步不会触发真实投稿。
4. 如需验证已经成功的真实发送步骤，进入“排期计划”，只使用任务右侧的单条“立即发送 / 转换并发送”，并继续先选“仅自己可见”做灰度。
5. 若出现二维码、短信、验证码、滑块、风控或结果不确定，任务应进入 `NEED_REVIEW`，先人工核对，不能连续重试。

## 2026-07-19 抖音立即发送真实验收结果与下一步

1. “电影院递纸巾”真实私密灰度已经完成，成功任务为 `pub_92aeb980cd804fe3883e3539d33ad5fe`，状态是 `PUBLISHED / confirmed_success`。
2. 抖音作品管理页已找到准确标题，卡片明确显示锁和“私密”；作品没有自动删除，请用户自行决定保留或删除。
3. 原失败记录及两次点击前停止的诊断记录均保留，用于追溯；结果不确定任务 `pub_1138f3bb8d07445ea42004493e61de66` 仍未触碰。
4. 当前日常启动方式已经更新为 Docker 自动联动；本段中的真实灰度结果仍保留，启动操作以文档顶部 2026-07-23 说明为准。
5. 后续正式发送仍先用“仅自己可见”做单条灰度；遇到二维码、短信、验证码、滑块、风控或结果不确定时，窗口保留 10 分钟且任务进入 `NEED_REVIEW`，人工核对前不得重试。

## 2026-07-18 发送中心修复后的单条抖音验收

1. 打开 `http://127.0.0.1:8001/publish` 并按 `Ctrl + F5`，确认顶部“当前只处理抖音”，内容准备、排期、执行记录和账号抽屉均只显示抖音数据。
2. 打开账号管理；“康熙来了”如果数据库登录正常，应自动显示“正常”和“打开创作者中心”，不需要反复刷新或点击检查。登录窗口打开后会先显示“等待登录完成”，完成登录后自动变回“正常”。
3. 7 条旧排期应显示在抖音“执行记录”的“需人工复核”中，错误码为 `legacy_schedule_requires_confirmation`；它们没有被上传，也不会自动补发。
4. 真实验收只选择标题包含“胡瓜老婆丁柔安搭捷运公车”的抖音任务，先在内容准备中把可见范围设为“仅自己可见”并保存；不要选择 B站任务。
5. 核对确认框明确写“抖音”、账号为“康熙来了”、标题和可见范围正确后，再由用户本人逐条点击“转换并发送”。旧记录会保留，新任务才会进入 Windows Chrome Worker。
6. 如果出现二维码、短信、验证码、滑块、风控或结果不确定，停止自动流程并保留 `NEED_REVIEW`；先到抖音创作者中心人工核对，不能直接重复发送。
7. B站必须切换到“B站”后单独操作；名为“抖音主账号”的旧记录实际属于 B站，只会显示在 B站区域，本次不要领取或发送。

## 2026-07-18 Worker 端口修复后的操作顺序

1. 在项目目录运行 `.\scripts\start_publish_worker.ps1`；重复运行时，脚本会安全停止旧 Worker、等待 `8765` 端口释放，再启动新 Worker。
2. 成功时应看到“发布 Worker 已启动”和“Docker 已同步完成”，不应再出现 `WinError 10048`。
3. 打开 `http://127.0.0.1:8001/publish`，点击“重新检测”，确认“Windows Worker”显示“正常”。
4. 后续如果提示端口被“其他程序”占用，请保留完整报错；脚本不会自动关闭无法确认身份的程序。

## 2026-07-16 立即发送修复后的操作顺序

1. 打开 `http://127.0.0.1:8001/publish`，进入“执行记录”。本次旧任务会显示“账号‘康熙来了’尚未登录”，不会再显示可直接执行的“立即发送”。
2. 点击“打开登录窗口”，在弹出的账号专属 Chrome 中人工完成抖音登录；二维码、短信、验证码或平台风控必须由用户本人处理。
3. 登录完成后回到“账号管理”，点击该账号的“检查状态”。只有状态变成“正常”，旧任务才会显示“修复并发送”。
4. 点击“修复并发送”时，系统会保留原 `NEED_REVIEW` 历史，另建一条 Windows Chrome 投稿任务。不要手动删除原记录，它用于避免重复投稿和追溯失败原因。
5. 先用单条、确认可投稿的短视频做灰度；抖音建议设为“仅自己可见”。状态应按 `SCHEDULED → PUBLISHING → PUBLISHED` 推进，并在执行记录出现平台链接。
6. B站目前没有账号，因此 B站任务会显示“新增账号”；需要先创建 B站账号并完成独立登录，不能复用抖音账号。
7. 如果 Worker 显示未连接，页面主按钮会改为“连接 Worker”。在项目目录运行 `.\scripts\start_publish_worker.ps1`，回到页面点击“重新检测”，任务状态不会因这次失败而改变。

## 2026-07-16 平台月历与 Worker 修复后的下一步

1. Windows Worker 与 Docker 当前已经连接；平时如果重启电脑，只需在项目目录运行 `.\scripts\start_publish_worker.ps1`，脚本会自动同步正在运行的 Docker 页面，不再需要额外重启命令。
2. 打开 `http://127.0.0.1:8001/publish`，确认调度器卡片显示“Windows Worker：正常”；如果未连接，按卡片黄色区域中的命令启动，再点击“重新检测”。
3. 进入“排期计划”，分别点击“抖音排期”和“B站排期”，确认月历与下方任务清单同步切换；点击月历中的任务会定位到对应清单卡片。
4. 当前只验证了账号登录窗口可以正常打开，尚未替用户完成平台登录。下一步由用户在专属 Chrome 中人工完成二维码、短信或平台验证，再回到账号管理点击“检查状态”。
5. 真实投稿仍先做单条灰度：抖音建议选择“仅自己可见”；B站测试稿可能进入审核或展示，最终投稿前必须人工确认。

## 2026-07-15 v1.5.0 真实单条灰度发布（下一步优先做）

1. 在项目根目录运行 `\.\scripts\start_publish_worker.ps1`。成功时会显示 Worker 地址和健康检查结果；脚本生成的 Token 只写本地 `.env`，不会提交 Git。
2. 如果 Docker 页面已经运行，脚本会自动同步连接配置；只有首次安装或镜像依赖变化时才需要运行 `docker compose up --build`。
3. 打开 `http://127.0.0.1:8001/publish`，进入“内容准备 → 账号管理”，为抖音和 B站分别新增账号。
4. 点击“打开登录窗口”，在弹出的专属 Chrome 中人工完成二维码、短信或平台验证；回到页面点击“检查登录”，应显示“正常”。
5. 先选一条用户确认可投稿的短测试片。抖音建议设置“仅自己可见”；B站稿件即使测试也可能进入审核或公开展示，点击最终投稿前必须再次确认。
6. 核对标题、正文/简介、话题/标签、封面和账号；B站还要核对分区、原创/转载，转载必须填来源。
7. 点击“立即发送”，预期状态为 `WAITING → SCHEDULED → PUBLISHING → PUBLISHED`；平台链接应出现在执行记录。
8. 再用另一条测试片预览未来北京时间排期，确认逐条时间后应用；重启应用，任务仍应保留并在到点后走同一个 Publisher。
9. 如果出现验证码、登录失效、风控或结果不确定，正确状态是 `NEED_REVIEW`。先打开平台创作者中心核对，确认未发布后才能标记失败并创建重试任务。
10. `manual_export` 只能显式选择，正确结果是 `EXPORTED`；它不会变成 `PUBLISHED`，真实发送失败也不会自动导出发布包。

## 后续风险收敛
1. 用抖音、B站各做一次单条灰度，记录平台页面选择器因改版产生的失败点。
2. 继续观察调度器健康状态的 5 秒前端刷新是否稳定；当前已提供异常提示、启动命令和手动重新检测。
3. 观察 Windows Worker 的 `execution_phase` 和本地日志，确认上传前连接重试与上传后禁止重试符合真实网络环境。
4. 观察历史数据库迁移备份与重复任务取消结果，确认后再考虑更严格的数据清理工具；不要删除已发布记录。

## 2026-06-25 全自动流程修复后怎么测试
1. 打开 `http://127.0.0.1:8001/tasks/new`，按 `Ctrl + F5` 强制刷新一次。
2. 选择视频，填写“单条切片最长”和“候选片段数量”，勾选“新建后自动跑完整流水线”。
3. 点击“创建并自动处理”只负责提交任务；进入任务详情后不应再出现手动“开始处理”，页面会自动刷新处理状态。
4. 正常流程应自动完成音频提取、转写、AI 分析、候选片段恢复/选取、原片切割、文案和发布任务创建。
5. 如果旧任务停在“AI 结果待检查”，点击“继续全自动流程”；系统会读取最近一次 AI 历史结果继续切片，不会重新调用 AI。
6. 打开 `/publish`，新生成的任务应先显示“等待处理 / 未排期”，不会在没有发布时间时被调度器执行。
7. 勾选一条或多条未发布任务，在“批量发布计划”设置起始时间、间隔小时和每日时段，然后点“应用发布计划”。
8. 跨过每日结束时间的下一条任务应自动排到次日开始时间；点击“清除所选排期”后应恢复为“未排期”。
9. 有风险标记的任务仍保持“需复核”；人工复核通过后，有发布时间则进入定时队列，没有发布时间则回到等待状态。

## 2026-06-23 v1.4.0 定时发送怎么测试
1. 在项目目录启动后台：`.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8001`。
2. 打开 `http://127.0.0.1:8001/tasks/new`，新建任务并勾选“新建后自动跑完整流水线”。
3. 等任务自动完成准备视频、转写/读取文本、AI 分析、自动选片、原片切割、生成标题文案和创建待发送任务。
4. 该段是 v1.4.0 历史测试记录。当时默认 `manual_export`；v2.1.0 当前默认 `local_browser`，到点后会调用 Windows Worker 真实投稿。手动扫描命令仍是 `.\.venv\Scripts\python.exe -m app.publish_scheduler run-once`。
5. 发布包默认在 `outputs/publish_packages/{task_id}/{clip_id}/`，应能看到 `clip.mp4`、`title.txt`、`caption.txt`、`hashtags.txt`、`cover_text.txt`、`publish_plan.json`、`metadata.json`。
6. 打开 `/publish`，在发布记录里查看 `SCHEDULED`、`PUBLISHING`、`PUBLISHED`、`FAILED`、`NEED_REVIEW` 等状态；也可以访问 `/api/publish/queue/snapshot` 查看队列快照。
7. 失败任务可以调用 `POST /api/publish/jobs/{job_id}/retry` 重试；立即发布可以调用 `POST /api/publish/jobs/{job_id}/publish-now`；取消和跳过分别调用 `/cancel`、`/skip`。
8. `NEED_REVIEW` 表示任务带风险标记或需要人工复核，不会自动发布；复核后调用 `POST /api/publish/jobs/{job_id}/approve-review` 可回到 `SCHEDULED`。
9. 本轮仍然跳过加字幕、烧录字幕和字幕叠加，自动发布使用 `05_clips/` 的原片切割结果。

## v1.5.0 仍需真实环境确认

1. 自动测试已经覆盖 Registry、状态机、时间、Worker 客户端和页面流程，但没有使用真实账号点击平台最终投稿。
2. 抖音/B站平台页面会持续改版，真实灰度时若选择器变化，必须按错误阶段修正，不能伪造成功。
3. 作品审核、限流、账号权限与平台规则属于外部状态；系统只在取得成功证据时写 `PUBLISHED`。
4. opencli 仅作为显式兼容模式保留，默认关闭，不再作为立即发送专用链路。

## 2026-06-23 v1.3.0 全自动流水线怎么测试
1. 启动本地后台后打开 `http://127.0.0.1:8001/tasks/new`。
2. 选择一个本地视频，设置候选片段数量和单条切片最长，勾选“新建后自动跑完整流水线”。
3. 点击“创建并自动处理”后进入任务详情页，观察状态从 `CREATED` 依次推进到准备视频、转写、AI 分析、自动选片、原片切割、标题文案、发送队列、发布任务。
4. 本轮全自动模式会跳过加字幕：任务目录中应生成 `05_clips/` 原片裁切结果，不应因为全自动流程生成新的 `06_subtitled/` 带字幕成片。
5. 查看任务目录的 `analysis/task_summary.json`，确认成功切片数量、失败切片数量、文案数量、发布任务数量和需人工复核切片都有记录。
6. 打开 `/publish`，确认自动创建的发布任务已进入队列；有 `risk_flags` 的任务会显示为需人工复核，不会自动发送。

## v1.4.0 下一轮建议
1. 接真正的定时发送调度器：按 `publish_jobs.scheduled_at` 找到到点任务，再交给 opencli 或未来平台 API 执行。
2. 增加发布任务的重试、暂停、跳过和人工确认队列，确保风控、验证码、登录失效都停在人可处理的边界。
3. 给全自动任务增加更清晰的前端进度轮询和失败步骤重试提示，减少用户刷新页面找状态。
4. 如需恢复字幕能力，建议作为全自动模式的独立可选开关，不要默认烧录字幕。

## 2026-06-15 v1.3 合并后怎么检查
1. 打开项目后先确认左侧显示 `v1.3 分支整合版`。
2. 启动 Docker 主页面：`docker compose up --build`。
3. 打开 `http://127.0.0.1:8001/health`，确认返回 `status: ok`。
4. 按真实流程检查一条任务：任务详情、转写、AI 分析、片段审核、生成切片、自动字幕、发送中心。
5. 如果要测试抖音 / B站发送，继续先只选 1 条任务，遇到验证码、登录失效、风控或平台人工确认时手动处理。

## v1.3 下一步我建议做什么
1. 先观察 `master` 上的 v1.3 是否稳定，不急着继续大改架构。
2. 下一轮优先做小范围体验增强：字幕批量处理、字幕文本编辑、发送失败重试体验、任务文件入口。
3. 旧分支清理完成后，后续新功能统一从最新 `master` 新建分支，避免再次出现分支互相不包含的问题。

## 2026-06-09 v1.2 收拢后怎么测试
1. 启动 Docker 主页面：`docker compose up --build`。
2. 打开 `http://127.0.0.1:8001`，确认左侧显示 `v1.2 MVP 全流程`。
3. 打开 `http://127.0.0.1:8001/health`，确认返回 `status: ok`。
4. 选择一条真实任务，按顺序检查：任务详情、AI 分析、片段审核、生成切片、字幕推送、发送中心。
5. 如果要测试平台发送，先运行 `.\scripts\start_docker_opencli.ps1`，再打开 `/publish`，只选 1 条任务点“发送此条”。
6. 发送过程中如果出现验证码、登录失效、风控或平台人工确认，按平台页面处理；系统不会绕过这些限制。

## v1.2 下一步我建议做什么
1. 先完整跑一条真实长视频：“上传视频 -> 开始处理 -> 远程 AI 分析 -> 片段审核 -> 生成切片 -> 自动加字幕 -> 刷新发送队列”。
2. 再分别测一条抖音和一条 B站发送任务，确认标题、简介、话题、封面和平台结果都符合预期。
3. 如果 v1.2 稳定，下一阶段再做小范围增强，例如字幕批量处理、字幕文本编辑、发送失败重试体验和更细的视频元信息展示。
4. GitHub 网络恢复后，把本地 `master` 推送到 `damingishere-coder/ai-clip-workflow`，并按新规则创建 PR；不要自动合并 PR 或删除分支。

## 2026-06-09 Docker 8001 + opencli 辅助服务怎么测试
1. 以后只打开 Docker 主页面：`http://127.0.0.1:8001`，不要再切到 `8002`。
2. 在项目目录运行：`.\scripts\start_docker_opencli.ps1`。
3. 这个脚本会做三件事：检查 Windows opencli、启动 opencli 辅助服务、刷新 Docker。
4. 脚本正常时会自动打开 `http://127.0.0.1:8001/publish`；如果没自动打开，就手动打开这个地址。
5. 页面打开后按 `Ctrl + F5` 强制刷新。
6. 正常情况：发送中心顶部不再提示 opencli 缺失；如果提示“还没有连接到 Windows opencli 辅助服务”，再运行一次 `.\scripts\start_docker_opencli.ps1`。
7. 只勾选一条任务，点击“发送此条”，确认页面出现“正在发布”状态框，Chrome 会打开对应平台投稿页。

## Docker opencli 下一步我建议做什么
1. 先只测一条抖音任务，不要一次发送全部。
2. 如果 Docker 页面能打开平台页，说明“8001 页面 + Windows opencli 辅助服务”链路打通了。
3. 如果脚本提示“没有检测到 opencli”，再执行 `where opencli`，把输出发给我继续排查安装问题。

## 2026-06-08 全页面美化后怎么测试
1. 启动本地后台。推荐先继续用你平时的 Windows 本机服务端口，如果没有启动，可以在项目目录运行 `.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8010` 临时预览。
2. 打开 `http://127.0.0.1:8010`，如果你用的是 8001 或 8002，就把地址里的端口换成自己的。
3. 按 `Ctrl + F5` 强制刷新，确认浏览器加载到新版 `20260608-apple-redesign` 样式。
4. 依次打开：工作台、任务列表、新建任务、片段审核、字幕推送、发送中心、系统状态。
5. 重点看三件事：左侧导航是否清爽，卡片和表格是否更像设计图，右上角“新建任务”按钮是否保持单行。
6. 打开一个真实任务详情页，再进入它的片段审核页，确认桌面下仍是左侧候选片段、右侧视频预览的双栏布局。
7. 如果你用手机或窄窗口测试，确认顶部导航变成横向可滑动条，首屏能看到页面标题和主操作。

## 全页面美化后下一步我建议做什么
1. 先用桌面浏览器把 7 个主页面都快速过一遍，只看外观，不急着跑完整视频流程。
2. 如果整体风格满意，下一轮可以单独微调“图标系统”和“任务详情顶部操作区”，让按钮更像专业剪辑后台。
3. 如果你觉得某个页面仍然太挤，优先截图发给我，我按单页继续精修，不影响后端处理链路。

## 2026-06-08 B站自动发送修复后怎么测试
1. 重启 Windows 本地 `8002` 后台服务，确保加载到本次修改后的代码。
2. 打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5` 强制刷新页面。
3. 确认 Chrome 里已经登录 B站账号，并且 OpenCLI 扩展处于可用状态。
4. 在发送中心只选一条 B站任务，先不要点“开始发送全部”。
5. 点击这条任务的“发送此条”，正常情况会出现居中的“正在发布”框。
6. 在 B站创作中心观察：如果出现“本地浏览器存在未提交的视频”，程序会点“不用了”；随后上传本次切片视频。
7. 上传完成后，程序会使用 B站页面生成的推荐封面，标题使用发送中心标题，创作声明选择“内容无需标注”，简介填写发送中心的“正文 / 简介”，标签保持 B站默认/推荐标签。
8. 最后程序会点击“立即投稿”，并等待“投稿成功 / 提交成功 / 已提交审核 / 审核中”等成功信号；只有等到这些信号，本地任务才会标记为已发布。
9. 投稿成功后确认 Chrome 顶部的 OpenCLI Browser 页面仍然保留，不会自动关闭。
10. 如果出现验证码、登录失效、风控或平台人工确认，请先按平台页面处理或截图发回来；项目不会绕过这些限制。

## B站自动发送下一步我建议做什么
1. 先只测 1 条 B站任务，确认视频、封面、创作声明、简介和最终投稿结果都对。
2. 第一条成功后，再连续测 2 条不同切片，观察 B站推荐封面生成速度和“内容无需标注”是否稳定选中。
3. B站链路稳定后，再回头做“批量发送全部”的真实测试；不要一开始就批量，方便定位单条失败原因。

## 2026-06-07 抖音自动发送修复后怎么测试
1. 重启 Windows 本地 `8002` 后台服务，确保加载到本次修改后的代码。
2. 打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5` 强制刷新页面。
3. 在发送中心只选一条抖音任务，先点一下这张卡片，确认右侧手机预览会显示这条任务的封面、标题、话题和正文简介。
4. 点击“发送此条”，正常情况会立刻出现居中的“正在发布”框。
5. 在抖音创作者页面观察：标题和作品描述应已填写，右侧投稿预览应可见，AI 推荐封面生成后会自动选择第一张并点击“确定”确认应用。
6. 最后程序会点击发布，并等待平台出现“发布成功 / 已提交审核 / 审核中”等结果；只有等到这些成功信号，本地任务才会标记为已发布。
7. 发布成功后观察 Chrome 顶部，本次自动投稿打开的 `OpenCLI Browser` 标签应自动关闭，不会继续越堆越多。
8. 如果出现验证码、登录失效、风控或平台人工确认，请先按平台页面处理或截图发回来；项目不会绕过这些限制。
9. 如果失败，请把发送中心卡片上的红色错误完整发给我；新的错误会区分描述未写入、预览未就绪、封面确认未点击、误点页面 logo 跳转和发布结果未确认。

## 抖音自动发送下一步我建议做什么
1. 先只测 1 条抖音任务，不要一次点“开始发送全部”，这样如果平台弹窗变化更容易定位。
2. 第一条真实发成功后，再连续测 2 条不同切片，确认封面确认弹窗、发布中状态和 OpenCLI 标签自动关闭都稳定。
3. 抖音链路稳定后，再单独回头验证 B站发送，不把两个平台的问题混在同一轮里。

## 2026-06-07 DeepSeek Pro 远程分析修复后怎么测试
1. 刷新或重新打开本地后台：`http://127.0.0.1:8002`。
2. 打开任务 `测试5 - 康熙来了`，也可以直接访问 `http://127.0.0.1:8002/tasks/d38b9158aba1/clips/review`。
3. 正常情况：页面能看到 12 条候选片段；这条任务已经由远程 DeepSeek Pro 重跑成功，不再停在“AI Chat Completions 响应中没有文本内容”。
4. 如果你想验证新任务，找一个已完成转写的任务，点击“远程 AI 分析”，等待运行日志出现“AI 分析完成”。
5. 如果仍失败，把运行日志最后 20 行发给我；新的错误会带 `finish_reason`、返回字段和推理长度，方便继续定位。

## DeepSeek Pro 远程分析下一步我建议做什么
1. 先进入 `测试5 - 康熙来了` 的片段审核页，看 12 条候选片段的时间点和标题是否符合你的切片思路。
2. 质量满意后，再点击生成切片，检查输出视频是否能正常播放。
3. 后续如果同类长视频仍偶发超时，再把远程分析超时时间从 120 秒提高到 300 秒；这次先不扩大配置改动。

## 2026-06-06 三类 AI 接口配置怎么测试
1. 重启或刷新本地后台服务，打开 `http://127.0.0.1:8001/system`。
2. 在页面中间找到“三类 AI 接口配置”，确认能直接看到 `1. 音频转写`、`2. 分析文字稿，生成候选切片`、`3. 发送中心生成发布文案`。
3. 检查三个 API Key 输入框是不是普通文本框，并且会完整显示你自己填写过的 Key。
4. 修改“发送中心生成发布文案”的模型，例如改成 `deepseek-chat`，点击“保存三类 AI 接口配置”。
5. 保存后刷新页面，确认发布文案模型仍是你刚才填写的值；同时 `.env` 里的 `VOLCENGINE_ASR_API_KEY` 不应被删除。
6. 新建或打开一个未完成转写的任务，先保证“音频转写”里火山引擎 Key 已填写，再点击“开始处理 / 继续处理”。
7. 正常情况：不会再因为系统状态页保存过配置而报“缺少火山引擎转写密钥”。

## 三类 AI 接口配置下一步我建议做什么
1. 先只用一个短视频验证火山转写，不要一开始就跑很长的视频。
2. 转写跑通后，再用同一个任务点击“远程 AI 分析”，确认候选切片能生成。
3. 最后打开发送中心，用“AI 补齐标题/话题”验证发布文案接口是否按独立模型生成内容。

## 2026-06-06 发送中心 DeepSeek 发布文案模型怎么测试
1. 重启或刷新本地后台服务，确保加载到这次修改后的代码。
2. 打开 `http://localhost:8001/system`，找到“三类 AI 接口配置”。
3. 在“3. 发送中心生成发布文案”区域修改模型，然后点击“保存三类 AI 接口配置”。
4. 保存后检查项目根目录 `.env`，应出现 `AI_PUBLISH_REMOTE_MODEL=你选择的模型名`，并且火山转写 Key 不应被删除。
5. 打开 `http://localhost:8001/publish`，先确认已经有生成好的切片，再点击“AI 补齐标题/话题”。
6. 正常情况：发送队列会生成标题、平台话题和简介；即使“默认分析方式”选的是本地模型，发送文案也会固定使用发布文案远程接口。
7. 如果 DeepSeek Key 不对或接口失败，页面不应崩溃；它会退回本地规则生成标题和话题，错误会写进任务的 metadata。

## 发送中心发布文案下一步我建议做什么
1. 先用 `deepseek-v4-flash` 做 1-2 条短视频文案测试，确认标题和话题风格适合抖音 / B站。
2. 如果标题质量不够，再切到 `deepseek-chat` 或 `deepseek-reasoner` 对比效果和速度。
3. 后续可以把发布文案 Prompt 做成可视化编辑，并进一步支持抖音、B站分别配置不同文案模型。

## 2026-06-06 远程失败不自动本地怎么测试
1. 先重启本地后台服务，确保运行的是这次修改后的代码。
2. 打开任意一个还没有完成转写的任务详情页。
3. 临时让远程转写不可用，例如先把 `.env` 里的火山引擎 Key 改成错误值，或断开远程转写配置后再点“开始处理 / 继续处理”。
4. 正常情况：音频提取完成后，转写会失败并停住；页面会显示失败原因，不会自动开始本地 faster-whisper。
5. 此时页面顶部会出现“改用本地模型转写”按钮。点击后会先弹确认框；确认后才会开始本地转写。
6. 如果你修好了远程配置，也可以点“重新远程转写”，它会继续优先走远程。

## 远程失败确认机制下一步我建议你做什么
1. 先用一个很短的视频做测试，不要拿 40 分钟长视频验证按钮逻辑。
2. 确认远程失败时不会自动吃电脑性能跑本地模型后，再把 `.env` 里的远程 Key 改回真实值。
3. 后续如果远程 AI 分析失败，页面也会停住并提示原因；需要本地模型时再手动点“本地 AI 分析”。

## 2026-06-04 AI 置信度分数修复后怎么测试
1. 先重启本地后端服务，确保运行的是这次修复后的代码。
2. 打开刚才报错的任务详情页。
3. 在 AI 分析区域重新点击远程 AI 分析，等待右侧运行日志刷新。
4. 正常情况：不会再因为 `confidence_score` 显示 8.9、7.8 这类分数而报 `Input should be less than or equal to 1`，而是进入候选片段生成和审核流程。
5. 如果仍然失败，把任务详情页右侧运行日志最后 20 行发给我；这时优先判断是否还有别的字段格式、时间范围或接口输出截断问题。

## AI 置信度分数修复后下一步我建议你做什么
1. 先用同一个失败任务重新跑一次远程 AI 分析，不需要新建任务。
2. 跑成功后进入片段审核页，确认候选片段的标题、时间、摘要和置信度都能正常显示。
3. 如果候选片段质量可以，再点击生成切片，检查输出短视频能否正常播放。

## 2026-06-04 运行日志精简后怎么测试
1. 打开或刷新任务详情页：`http://localhost:8001/tasks/fba336c5523b`。
2. 看页面右侧，确认标题只显示“运行日志”，不再出现“日志 / 元信息”和一长串源视频路径、任务目录、分析文件路径。
3. 在 AI 分析区域点击一次“远程 AI 分析”或“本地 AI 分析”，确认右侧运行日志仍然会刷新。
4. 如果你只是检查页面外观，不想真的重跑 AI，可以先只刷新页面确认右侧结构已经变简单。

## 运行日志精简后下一步我建议你做什么
1. 先确认右侧日志区域是不是比之前清爽，尤其是长视频路径不再挤占页面。
2. 如果以后还想看源视频路径、任务目录这些信息，下一步可以做一个“任务文件”小入口，点击后再展开，不默认占用日志区域。

## 2026-06-04 浏览器标签页和收藏夹 logo 怎么测试
1. 先重启或等待本地后台自动刷新，确保后端加载到新的 `/favicon.ico` 路由。
2. 打开 `http://127.0.0.1:8001`，看浏览器标签页标题左边是否出现牛马小图标。
3. 如果 Chrome 还显示旧图标，先按 `Ctrl + F5` 强制刷新；收藏夹图标有缓存，必要时删除旧收藏后重新收藏一次。
4. 直接打开 `http://127.0.0.1:8001/favicon.ico`，正常情况会看到牛马图标或浏览器直接下载/展示这个图标文件。

## 浏览器图标更新后下一步我建议你做什么
1. 先确认标签页小图标是否够清楚，尤其是 16x16 很小的时候能不能看出是牛马头像。
2. 如果太糊或太复杂，下一步可以单独做一个更简化的“小尺寸专用 favicon”，不影响左侧导航的大 logo。

## 2026-06-04 牛马片场品牌更新后怎么测试
1. 先重启本地后台服务，确保页面加载的是新配置和新静态资源。
2. 打开 `http://127.0.0.1:8001`，看左上角是否显示牛马吉祥物 logo、`牛马片场` 和 `本地 AI 高光生产后台`。
3. 如果用 Docker 启动，打开 Docker Desktop，确认项目、镜像和容器名称显示为 `niuma-studio`。
4. 打开 `README.md` 和 `docs/PROJECT_GUIDE.md`，确认当前项目名已经是“牛马片场 / NiuMa Studio”。
5. 随便打开一个旧任务详情页，确认旧视频、转写、AI 分析和切片路径没有因为品牌更新而丢失。

## 牛马片场品牌更新后下一步我建议你做什么
1. 先只检查首页左上角品牌区，不急着跑完整视频流程。
2. 如果 logo 在你的屏幕上显得太可爱、太大或太小，下一步优先微调 logo 尺寸和侧边栏间距。
3. 品牌确认满意后，再决定是否把 E 盘历史目录 `E:\直播间切片工作流存储` 安全迁移为新名字；这一步要单独做备份和迁移计划。

## 2026-06-04 AI 远程分析旧字段修复后怎么测试
1. 先重启本地后端服务，确保运行的是这次修复后的代码。
2. 打开之前出现 `AI 返回非法 JSON` 报错的任务详情页。
3. 在 AI 分析区域再次点击远程 AI 分析，等待右侧运行日志刷新。
4. 正常情况：页面不再提示 `clip_id` 或 `spread_value` 字段缺失，而是生成候选片段，并可以进入片段审核页。
5. 如果仍然失败，把任务详情页右侧运行日志最后 20 行发给我；下一步优先判断是 AI 输出被截断、时间范围不合规，还是模型接口本身返回异常。

## AI 远程分析旧字段修复后下一步我建议你做什么
1. 先用同一个报错任务重新跑一次远程 AI 分析，确认这次能生成候选片段。
2. 跑成功后进入片段审核页，先检查标题、摘要、传播价值和剪辑建议是否都能正常显示。
3. 如果候选质量可以，再点击生成切片，检查输出短视频能否正常播放。
4. 如果候选片段内容仍然不理想，下一步再微调 Prompt；这次先确认 JSON 字段兼容问题已经解决。

## 2026-06-02 DeepSeek AI JSON 修复后怎么测试
1. 先重启本地后台服务，确保运行的是这次修复后的代码。
2. 打开刚才报错的任务详情页，进入“AI 分析”板块。
3. 确认当前 Prompt 方案仍然选择 2 号“康熙来了综艺短视频切片专家”。
4. 点击“远程 AI 分析”，等待右侧运行日志刷新。
5. 正常情况：页面不再停在 `AI 返回非法 JSON，安全重试后仍失败`，而是生成候选片段摘要，并可以点击进入片段审核页。
6. 如果仍失败，把任务详情页右侧运行日志最后 20 行发给我；这时优先判断是输出被截断、字段缺失、时间范围不合规，还是 DeepSeek 账号 / 模型权限问题。

## DeepSeek AI JSON 修复后下一步我建议你做什么
1. 先用同一个《康熙来了》任务重新跑一次远程 AI 分析，确认 2 号 Prompt 可以稳定生成候选片段。
2. 跑成功后进入片段审核页，不急着手动改，先看候选片段是否包含铺垫、爆点、反应和自然收尾。
3. 如果候选质量可以，再点击“生成切片”，检查输出短视频是否能正常播放。
4. 如果候选片段仍然太短或边界不自然，下一轮优先微调 2 号 Prompt 的片段边界要求，而不是继续改 JSON 解析。

## 2026-06-02 停止转写卡住怎么测试
1. 先重启本地后台服务，确保运行的是这次修复后的代码。
2. 打开当前卡住的任务详情页：`http://localhost:8001/tasks/fba336c5523b`。
3. 刷新页面一次，等待 5 秒左右；如果之前只是遗留的“正在停止”状态，页面应自动变成“转写已停止，可以重新生成转写”。
4. 如果页面上还有“停止转写”按钮，再点一次；这次不应该继续卡住，应提示当前没有正在运行的转写任务，或显示已停止。
5. 确认任务状态不再显示“转写中”后，再按需要点击“重新生成转写”重新跑一次。

## 停止转写卡住修复后下一步我建议你做什么
1. 先用当前任务确认页面能从“正在停止”恢复出来。
2. 再新建一个短视频测试任务，开始转写后立刻点“停止转写”，确认它能在当前小段结束后变成已停止。
3. 如果长视频转写仍然很慢，下一步优先检查火山引擎转写配置和分段时长，不要反复刷新页面。

## 2026-05-30 火山引擎远程转写怎么测试
1. 重启本地后台服务，避免旧代码还在运行。
2. 打开任务“测试4”的详情页。
3. 点击“重新 AI 转写（火山引擎）”或页面里对应的远程转写按钮。
4. 看右侧日志：如果出现“正在请求火山引擎转写第 1/23 段”，后面继续出现“火山引擎已完成第 N/23 段”，说明远程转写已经恢复。
5. 等进度到 100%，确认页面生成了 `transcripts/transcript.md`，任务状态进入“待 AI 分析”。
6. 如果前面几秒是静音，日志短暂停留是正常的；程序现在会跳过无有效人声的小段，不会因为静音直接失败。

## 火山引擎远程转写下一步我建议你做什么
1. 先用“测试4”完整跑一次远程转写，不要中途点停止。
2. 转写完成后，再点击远程 AI 分析生成候选片段。
3. 如果远程转写仍报错，把任务详情页右侧日志最后 20 行发给我，我继续帮你定位是额度、密钥权限、音频格式，还是接口返回的新错误。

## 2026-05-30 分支合并后怎么检查
1. 当前已经在主分支 `master`，并且 `codex/subtitlefunction` 的内容已经合并进来。
2. 你可以在项目继续开发前，先打开常用页面做一次简单确认：任务列表、任务详情、字幕工作台、片段审核页。
3. 如果页面能正常打开，说明这次分支合并没有破坏本地后台。

## 分支合并后下一步我建议你做什么
1. 先不要删除 `codex/subtitlefunction` 分支；确认主分支运行正常后再决定是否清理。
2. 下一轮开发建议直接从 `master` 新建一个新的功能分支，避免继续在旧功能分支上叠加修改。

## 2026-05-27 项目文件夹改名与回收站怎么测试
1. 打开 Windows 文件资源管理器，进入 `E:\直播间切片工作流存储`。
2. 确认根目录现在能看到 `测试2 - 19min`、`测试3 - 康熙来了20160104` 和 `_回收站`，不再是那几个看不懂的短 ID 文件夹。
3. 打开 `_回收站`，确认里面有 `测试1` 和 `闭环验证任务`；这两个是之前页面里已经隐藏的项目，文件没有删除。
4. 重启本地后台后，打开 `http://127.0.0.1:8001/tasks`，确认任务列表里还能看到正常项目。
5. 点进 `测试2 - 19min` 或 `测试3 - 康熙来了20160104`，确认详情页能打开，视频、切片、字幕路径不报错。
6. 新建一个小测试项目，项目名随便填一个容易认的名字，上传一个短视频；创建成功后去 E 盘确认新文件夹直接叫这个项目名。
7. 在任务列表点击“移入回收站”，确认网页列表里项目消失，同时 E 盘对应文件夹进入 `_回收站`。

## 项目文件夹改名下一步我建议你做什么
1. 先用现有两个正常项目各打开一次详情页和切片预览，确认旧视频还能播放。
2. 再新建一个很小的测试项目验证新规则；确认无误后，你以后就可以直接按项目名去 E 盘找完成视频。
3. `_回收站` 里的内容系统不会自动删除，你确认不需要后再手动删。

## 2026-05-27 片段审核功能优化怎么测试
1. 重启本地服务后，打开一个已经生成 AI 候选片段的任务，进入 `/tasks/{task_id}/clips/review`。
2. 找到一条你不想要的候选片段，点击卡片右上方的“删除”，确认这张卡片会立刻从页面消失。
3. 刷新片段审核页，确认刚才删除的片段不会重新出现。
4. 继续点击“生成切片”，确认系统只会使用剩下的、仍然启用的候选片段。
5. 连续向下浏览多条候选片段，确认卡片比之前更紧凑，推荐理由和剪辑建议需要展开后再看。

## 片段审核功能优化下一步我建议你做什么
1. 先用一个测试任务删除 1 条明显不合适的 AI 候选片段，确认刷新后仍然隐藏。
2. 如果你希望误删后可以恢复，下一轮可以增加“显示已删除 / 恢复片段”入口；当前版本先按你的要求做成删除后不占页面位置。

## 2026-05-27 AI 片段完整性怎么验收
1. 重启本地服务后，打开任意任务详情页，进入“AI 分析”板块。
2. 切换到 `2 号` 或 `3 号` Prompt，确认其中一个名称是“综艺访谈完整上下文专家”；如果你之前编辑过 2 号方案，系统不会覆盖它，会把新版 Prompt 放到空的 3 号方案。
3. 新建综艺或访谈任务时，把“单条切片最长”保持默认 `5 分钟`，或按页面提示设置为 `4-6 分钟`。
4. 对同一条《康熙来了》视频分别用旧 Prompt 和“综艺访谈完整上下文专家”Prompt 跑 AI 分析，对比候选片段是否更完整地包含铺垫、爆点、反应和收尾。
5. 进入片段审核页，不先手动补剪，直接点击“生成切片”，检查输出视频是否明显减少前后文割裂感。
6. 如果日志提示某些 AI 片段短于 `45 秒`，先不要急着手动修，优先用“综艺访谈完整上下文专家”Prompt 或把单条最长调到 `4-6 分钟` 后重跑 AI。

## AI 片段完整性下一步我建议你做什么
1. 先固定用“综艺访谈完整上下文专家”Prompt 跑 3 条不同类型视频：一条直播干货、一条综艺、一条访谈，记录哪类仍然容易短。
2. 如果 2 号 Prompt 稳定后，下一轮再做“AI 分析完成后自动生成切片”的开关，让审核页逐步变成只回看不必操作。

## 2026-05-26 字幕页片段内剪切怎么验收
1. 打开 `http://localhost:8001/subtitles/f8e1edf6a57f`，进入字幕推送任务页。
2. 点击任意已生成短视频卡片里的“修改剪切”，确认弹窗播放的是这个短视频片段本身，而不是整段源视频。
3. 拖动黄色左右把手，点击“预览这一段”，确认视频只在选区内播放。
4. 点击“保存入点 / 出点”，再回到片段审核页确认该候选片段的起止时间已更新。
5. 在片段审核页重新生成切片，检查新输出视频是否只保留这次选中的片段范围。

## 字幕页片段内剪切下一步我建议你做什么
1. 先用一条 1 分钟左右的短片段试一次，只把开头或结尾裁掉 2-3 秒，确认保存后的审核页时间符合预期。
2. 如果你希望更像手机剪辑软件，下一轮可以继续补真实缩略图胶片；当前版本先把“只裁当前片段”和“保存时间换算正确”稳定下来。

## 2026-05-26 片段审核 noUiSlider 源监视器怎么验收
1. 打开 `http://localhost:8001/tasks/f8e1edf6a57f/clips/review`，刷新页面。
2. 点击任意候选片段的“编辑出入点”，确认弹窗下方时间轴变成更顺手的双手柄蓝色范围滑块。
3. 拖动左手柄和右手柄，确认入点、出点和“片段时长”会实时变化。
4. 点击 `1秒 / 5秒 / 15秒` 步长按钮，再点“入点提前 / 后移”或“出点提前 / 后移”，确认每次按当前步长调整。
5. 点击“设为入点 / 设为出点”，确认滑块会跟随视频当前播放位置。
6. 点击“预览当前片段”，确认视频从入点播放，到出点附近自动暂停。
7. 点击“应用到片段”，确认卡片里的开始时间和结束时间已更新；再点击“保存修改”，刷新后确认时间仍保留。

## 源监视器下一步我建议你做什么
1. 先挑一条 AI 截得不准的片段，只改这一条，确认拖动手感是否比之前自然。
2. 如果你觉得仍然不够像剪辑软件，下一步再考虑加缩略图胶片或键盘快捷键；当前版本先把“能精确拖入点 / 出点”稳定下来。

## 2026-05-25 抖音 + B站发布后台怎么验收
1. 打开 `http://127.0.0.1:8001/publish`，确认左侧导航有“发布中心”，页面能看到“抖音后台”和“B站后台”两个页签。
2. 先在抖音后台填写 Client Key、Client Secret、回调地址和接口地址，点击“保存配置”，再点击“测试配置”。
3. 没有真实开放平台账号前，可以先新增一个“手动账号”；如果要真实发布，必须先完成抖音授权并拿到 access_token / open_id。
4. 找到一条待发布切片，先点“保存草稿”或“创建人工发布任务”，确认下方发布记录出现新任务。
5. 不配置账号时点击“发布到抖音”，应该提示缺少账号或配置，不应该新增错误发布任务。
6. B站后台现在用于保存开放平台配置、账号和稿件字段；未拿到 B站开放平台投稿接口权限前，不要期待它真实上传成功。

## 发布后台下一步我建议你做什么
1. 先去抖音开放平台创建应用，把本地回调地址设置成 `http://127.0.0.1:8001/api/publish/douyin/oauth/callback`。
2. 拿到 Client Key / Client Secret 后填到发布中心，优先用一条很短、不重要的视频做真实发布测试。
3. B站这边先完成开放平台入驻和投稿接口权限申请，拿到官方接口地址、签名规则和分区 ID 后，再补齐真实 provider 调用。
4. 每次真实发布前只选一条切片测试，确认标题、标签、账号和视频版本都正确，再考虑批量任务。

## 2026-05-25 字幕工作台剪切弹窗怎么验收
1. 打开 `http://localhost:8001/subtitles/f8e1edf6a57f`，刷新页面。
2. 找到任意一条切片，点击“修改剪切”。
3. 确认弹窗里是源视频预览，下方时间轴像手机剪辑一样：左侧播放按钮、黑色帧条、粉色外框、黄色选区和左右两个黄色手柄。
4. 拖动黄色左手柄调整入点，拖动黄色右手柄调整出点；也可以直接改“入点 / 出点”输入框。
5. 点击“预览这一段”，确认视频会从入点播放，到出点附近自动停住。
6. 点击“保存入点 / 出点”，看到“已保存”的提示。
7. 点击“去片段审核重新切片”，进入片段审核页后点击“生成切片”；这一步完成后，字幕工作台里才会出现按新时间切出来的视频。

## 下一步我建议你做什么
1. 先只拿一条你不满意的切片测试，不要一次改很多条。
2. 调好入点 / 出点并保存后，务必回片段审核页重新生成切片。
3. 重新生成后再回字幕工作台，播放新切片确认开头和结尾是否对。

## 2026-05-25 片段审核源监视器怎么验收
1. 打开片段审核页 `/tasks/f8e1edf6a57f/clips/review`，刷新页面。
2. 点击任意候选片段的“编辑出入点”，确认会打开深色源监视器弹窗。
3. 点击播放，在合适位置分别点“设为入点”和“设为出点”，确认蓝色范围和时间码会变化。
4. 拖动蓝色范围左右手柄，确认入点 / 出点可以快速调整。
5. 点击“预览当前片段”，确认视频从入点播放，到出点附近自动暂停。
6. 点击“应用到片段”，确认卡片里的开始时间和结束时间已更新；再点击“保存修改”，刷新后确认时间仍保留。
7. 调整后点击“查看这一段转写”，确认读取的是当前页面上的新时间范围。

## 2026-05-25 运行日志浅色化后怎么验收
1. 打开任务详情页 `/tasks/f8e1edf6a57f`，刷新页面。
2. 看右侧“运行日志”，确认它已经不是黑底，而是浅色蓝白卡片。
3. 如果日志很多，拖动日志框内部滚动条，确认文字清晰、滚动正常。
4. 后续再点击远程 AI 分析或本地 AI 分析时，继续观察这个浅色日志框是否能持续刷新。

## 2026-05-24 AI 分析日志和进度条后
- 任务详情页现在可以边跑 AI 分析边看右侧运行日志，不需要再单独打开 `process.log` 文件。
- AI 分析按钮下方会显示进度条；长视频分析时如果停在“分段生成候选片段”，属于正常等待。
- 如果 AI 分析失败，先看页面右侧运行日志最后几行，再决定是检查 DeepSeek Key、余额、网络，还是改用本地 AI。

## AI 分析日志下一步优先测试
1. 打开一个已经完成转写的任务详情页。
2. 选择 Prompt 方案后点击“远程 AI 分析”或“本地 AI 分析”。
3. 确认按钮下方出现进度条，右侧“运行日志”持续刷新。
4. 分析完成后进入片段审核页，检查候选片段是否正常出现。

## 2026-05-24 长视频 AI 分析修复后
- 已把远程 DeepSeek 和本地 Ollama 都改成分段分析，默认每段约 3 分钟，适合继续验证 40 分钟和 1 小时以上的长视频。
- 已兼容旧 Prompt 返回的 `clip_key`、`viral_value` 等字段，当前 2 号“康熙来了综艺短视频切片专家”Prompt 也已更新为新 JSON 格式。
- 页面上 AI 失败提示会变短；如果还失败，优先看任务右侧日志或 `E:\直播间切片工作流存储\任务ID\logs\process.log`。

## 长视频 AI 下一步优先测试
1. 打开任务 `f8e1edf6a57f`，先点击“远程 AI 分析”，等待它按分段生成候选片段。
2. 成功后进入片段审核页，检查候选片段的标题、起止时间、推荐理由和传播价值是否合理。
3. 再点击“本地 AI 分析”做对比；本地 Ollama 会更慢，但不应再因为 `clip_key` 或 `spread_value` 字段失败。
4. 如果 40 分钟任务稳定，再上传或选择 1 小时以上视频，按同样流程验证转写完成后的 AI 分析。

## 2026-05-24 国内远程转写接入后
- 已把转写默认方案改为“火山引擎远程转写优先，远程失败后暂停并等待用户确认本地 faster-whisper”。
- 已新增 `.env` 配置项：`TRANSCRIPTION_PROVIDER=volcengine`、`TRANSCRIPTION_FALLBACK_PROVIDER=`、`VOLCENGINE_ASR_API_KEY`、`VOLCENGINE_ASR_RESOURCE_ID=volc.bigasr.auc_turbo`。
- 已新增火山引擎连通性测试脚本：`scripts/test_volcengine_transcription_connection.py`。
- 已新增“停止转写”按钮；如果误用本地模型开始转写，可以先停止，再重启服务后重新生成。
- 下一步先在火山引擎控制台开通豆包语音相关权限，拿到 App Key 后填入 `.env` 的 `VOLCENGINE_ASR_API_KEY`。

## 国内转写下一步优先做
1. 用真实任务音频运行 `.venv\Scripts\python.exe scripts\test_volcengine_transcription_connection.py "音频路径"`，确认火山引擎能返回中文转写。
2. 修改 `.env` 后先重启 FastAPI / Docker 服务，再对 40 分钟视频点击“重新生成转写”，观察速度是否明显快于本地 CPU。
3. 如果火山引擎 Key、余额或权限异常，先看页面错误；系统会暂停转写并显示“改用本地模型转写”，确认后才会使用本地模型。
4. 后续再按同一 Provider 接口接阿里云、腾讯云或讯飞，实现真正多家切换。

## 2026-05-24 自动加字幕功能完善后
- 已把 `/subtitles` 改成字幕任务列表；不同视频任务需要点进详情页 `/subtitles/{task_id}` 后分别处理。
- 已新增字幕样式表 `subtitle_style_presets` 和字幕任务表 `subtitle_jobs`，样式会保存到 SQLite。
- 已接入 FFmpeg 自动加字幕：生成 `.ass` 字幕文件和 `06_subtitled` 目录下的带字幕 MP4。
- 已为“修改剪切”增加弹窗预览，方便先看画面和拖动时间轴；实际保存剪切时间仍回片段审核页。

## 下一步优先做
1. 字幕批量处理：在单任务字幕页增加“一键处理本任务全部待加字幕切片”。
2. 字幕编辑：允许在生成前查看和修改每条切片抽取出的字幕文本。
3. 剪切弹窗保存：把弹窗里的起止时间滑块真正写回候选片段，再触发重新切片。
4. 打码区域：先做固定区域打码并保存到数据库，再接 FFmpeg filter。
5. 发布前确认：带字幕成片生成后，进入抖音 / B站标题、简介、标签和二次确认页面。

## 2026-05-24 v1.1 清理与呈现优化
- 已将项目口径更新为 `v1.1`：本地上传、转写、AI 分析、人工审核、自动切片全流程已走通。
- 已清理重复 UI 参考图根目录副本，保留正式文件 `docs/design/live_streaming_slicing_workflow_ui_16x9.png`。
- 已移除旧的切片占位接口，统一使用真实切割接口 `POST /api/tasks/{task_id}/process/cuts`。
- 已优化工作台、任务列表和字幕推送页，让“输出切片、待加字幕、待推送”更直观。
- 已修复 `docs/DATABASE_SCHEMA.md` 乱码，并同步当前真实表结构。

## v1.1 下一步优先做
1. 字幕添加：新增 `subtitle_jobs` 和 `subtitle_style_presets`，把字幕样式从浏览器本地保存改为保存到 SQLite。
2. 字幕渲染：从转写文本里按切片时间范围提取字幕，接入 FFmpeg 生成带字幕视频。
3. 字幕预览：在 `/subtitles` 展示“原切片 / 带字幕成片”对比，保留人工复核。
4. 一键推送：先做发布任务表和发布前确认页，再接抖音 / B站账号与平台接口。
5. 安全边界：自动发布必须保留二次确认、日志记录、失败重试和不明文保存账号密码。

## 2026-05-23 本次小改动：新增字幕、推送功能 demo
- 已新增“字幕推送”页面，切片生成后的短视频会进入这里作为待加字幕队列。
- 已预留视频预览、删除、返回修改剪切、自动加字幕、字幕样式、打码和抖音 / B站一键推送入口。
- 已新增计划书 `docs/SUBTITLE_AND_PUBLISH_PLAN.md`，后续按“字幕渲染 → 打码 → 发布任务”的顺序逐步接真实能力。
- 下一步建议先建 `subtitle_jobs` 和字幕样式表，把当前浏览器本地保存的字幕样式改为保存到 SQLite，然后接入 FFmpeg 生成带字幕视频。

## 2026-05-23 本次小改动：AI 分析预览和数量调整
- 已把 AI 分析预览改为可刷新保留，任务详情页会显示最近一次分析摘要。
- 已新增 AI 分析历史结果面板，可按“第 N 次分析”查看历史，并可恢复整次历史结果到片段审核。
- 已在 AI 分析区域新增候选片段数量输入框，默认 5 条，分析前可改为 1-50 条。
- 下一步用同一任务分别生成 3 条和 5 条结果，确认历史恢复后片段审核页候选内容同步变化。

## 2026-05-23 本次小改动：AI 分析 UI 与审核来源提示
- 已把任务详情页 AI Prompt 方案改成 1、2、3 号标签页，切换标签后只显示当前方案的名称和 Prompt 输入框。
- 已新增 AI 分析完成后的结果摘要框，展示实际 AI 来源 / 模型、候选片段数量、标题、开始时间、结束时间和视频长度，并提供“转到片段审核”按钮。
- 远程按钮当前统一显示为“远程 AI 分析”，本地 `.env` 已更新 DeepSeek-compatible 配置并通过连通性测试。
- Docker 已改成本地开发模式：后续修改 `app/` 或 `prompts/` 后，容器会读取本机文件；普通刷新网页即可看到 HTML/CSS/JS 变化，Python 改动会自动重载。
- 下一步用真实任务分别点击“远程 AI 分析”和“本地 AI 分析”验证：远程接口成功时审核页应显示远程模型；如果仍显示本地，优先检查远程 Key、余额、模型权限和网络连通性。

## 2026-05-23 本次小改动：Docker 名称统一
- 已把 Docker Compose 项目名统一为 `niuma-studio`，Docker Desktop 里不再继续使用目录名 `newproject2` 作为项目名。
- 已把构建镜像固定为 `niuma-studio:latest`，以后重新构建会使用明确镜像名，减少旧 `<none>` 镜像堆积带来的困惑。
- 已确认普通 `docker compose up` 只是启动容器，不会每次复制 2GB；只有重新构建才可能留下可清理的历史镜像。

## 2026-05-23 本次小改动：AI Prompt 方案
- 已把任务详情页的 AI 分析配置从“AI 偏好”升级为“AI Prompt 方案”，支持 1、2、3 号全局共用方案、方案命名、Prompt 编辑和当前任务选择。
- 1 号方案已内置默认直播切片分析专家 Prompt；远程 AI 分析前会二次确认，避免误点后覆盖已有候选片段。
- 下一步建议补充 2、3 号 Prompt 方案，例如强观点型、知识密度型或爆点型，并用真实长视频对比候选片段质量。

## 本次小改动
- 已按要求将 DeepSeek 远程默认模型改为 `deepseek-v4-flash`，远程地址仍为 `https://api.deepseek.com`，协议仍为 `chat_completions`。

## 2026-05-29 本次小改动：封面 MVP 方案添加
- 已在发布中心接入封面 MVP：选择切片、填写标题和封面时间点后，可以生成“视频截图 + 暗色遮罩 + 标题字”的本地封面。
- 封面文件会保存到任务目录 `07_covers/`，创建发布任务时会把封面路径写入 `publish_jobs.cover_file_path`。
- 当前先不调用 AI 生图，后续可作为可选模式补充到封面工具里。
- 下一步建议用一条真实切片测试：生成字幕成片后进入发布中心，选择“带字幕成片”，生成封面，确认预览图和发布记录都正常显示。

## 2026-06-02 本次小改动：发送中心 2.0
- `/publish` 已改为发送中心：不再配置开放平台 API、Client Key、Access Token 或 OAuth 账号，改为查看待发送队列并确认发送。
- 发送队列默认从已完成切片读取“切好的原片”、候选封面帧、AI 标题、AI 话题和平台状态，并生成抖音 + B站两条平台任务。
- 封面改为从视频中选帧：页面可生成多张候选帧并手动切换，不再默认做标题大字封面。
- 自动发送依赖 Chrome 已登录状态 + opencli 网页自动化；如果遇到验证码、登录失效或平台风控，任务会失败并等待人工处理，不绕过平台限制。
- 下一步建议先打开 `http://127.0.0.1:8001/publish` 或本轮测试地址 `http://127.0.0.1:8002/publish`，点击“刷新发送队列”，确认视频、标题、话题和平台卡片正常显示。

## 2026-06-04 本次小改动：修复 opencli 检测
- 发送中心现在可以识别 Windows npm 全局安装目录里的 `opencli.cmd`，当前本机检测结果为 `C:\Users\10578\AppData\Roaming\npm\opencli.cmd`。
- 如果页面还显示“没有检测到 opencli”，优先重启项目后台服务并刷新 `/publish`；如果仍然出现，再检查服务是否运行在 Docker 容器里，因为 Docker 容器内看不到 Windows 主机的 opencli。
- 下一步建议在 Windows 本机启动项目后进入 `/publish`，确认顶部 opencli 提示消失，再勾选一条任务测试“发送此条”。

## 2026-06-04 本次小改动：修复 opencli `--window` 参数
- 已修复发送中心调用 opencli 时 `unknown option '--window'` 的报错，原因是参数位置放错。
- 下一步继续在 Windows 本机版 `http://127.0.0.1:8002/publish` 测试“发送此条”；如果页面已打开但上传 / 填写失败，再根据具体错误继续调整选择器。

## 2026-06-04 本次小改动：修复抖音 `Not allowed` 上传错误
- 已修复抖音上传视频时报 `{"code":-32000,"message":"Not allowed"}` 的问题：视频上传改为从 `http://127.0.0.1:8002/media/...` 读取本地切片后注入抖音上传控件。
- 当前本机服务必须继续使用 `8002`，或在 `.env` 设置 `OPENCLI_LOCAL_BASE_URL` 为实际本机访问地址。
- 下一步继续测试抖音“发送此条”；如果后续卡在“作品描述 / 自主声明 / 发布按钮”，按新的错误信息继续微调表单填写步骤。

## 2026-06-04 本次小改动：修复标题输入框选择器
- 已修复上传视频后填写标题时报 `selector_ambiguous` 的问题：发送中心现在不会再用容易匹配到多个元素的标题 CSS 选择器直接填写。
- 抖音和 B站标题填写都会优先选择当前页面里可见、可编辑的标题输入框。
- 怎么测试：重启本地项目服务后，打开 `/publish`，先只勾选一条待发送任务，点击“发送此条”，观察视频上传完成后标题是否能自动填入。
- 下一步：如果标题已经能填入，再继续看“简介 / 标签 / 发布按钮”是否还报新错误；如果仍失败，把新的完整报错内容复制出来继续修。

## 2026-06-04 本次排查：旧后台进程导致旧报错
- 如果仍看到 `input[placeholder*='标题'],textarea[placeholder*='标题'] matched 2 elements`，说明浏览器调用到的很可能还是旧后台服务。
- 本轮已经重启 Windows 本机 `8002` 服务，并确认 `http://127.0.0.1:8002/publish` 可以打开。
- 怎么测试：在浏览器地址栏确认是 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5` 强制刷新，再只选一条任务点击“发送此条”。
- 下一步：先不要用 `8001` Docker 页面测试 opencli 自动投稿；Docker 里通常看不到 Windows 主机的 opencli 和 Chrome 登录态。

## 2026-06-04 本次小改动：修复简介填写截断
- 已修复简介/描述填写时 `filled: true` 但 `verified: false` 的问题：抖音和 B站简介现在也使用脚本填写，不再依赖 opencli 对 `contenteditable` 的严格精确校验。
- 怎么测试：重启 `8002` 后打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5`，只选一条任务点击“发送此条”，观察标题和简介是否都能填完。
- 下一步：如果简介能过，继续看是否卡在标签、发布按钮、原创声明或平台人工确认弹窗；出现新错误就把完整错误发出来继续修。

## 2026-06-05 本次小改动：修复抖音话题、AI封面和发布按钮
- 抖音发送时，“作品描述”现在只填写发送中心的正文 / 简介；平台 #话题会单独写成抖音页面里的蓝色话题块。
- 抖音封面现在等待并选择“AI智能推荐封面”的第一个可用推荐图，不再使用发送中心封面。
- 抖音发布按钮现在精确点击底部“发布”，不会再误匹配“高清发布”。
- 怎么测试：重启 `8002` 后打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5`，只选一条抖音任务点击“发送此条”；在抖音页面检查描述是否和发送中心一致、话题是否变成蓝色块、AI 推荐封面是否被选中。
- 下一步：如果出现 `douyin_ai_cover_not_ready`，说明抖音 60 秒内没有生成可用 AI 推荐封面；如果出现验证码、风控或人工确认弹窗，先记录错误，不绕过平台限制。

## 2026-06-05 本次小改动：优化片段审核播放器和操作区
- 片段审核页右侧播放器已放大到页面内容区约三分之一，并保持在当前画面内，向下滑动候选片段列表时不用再回到页面顶部看视频。
- “保存修改”“生成切片”“去字幕推送”已改成更明显的操作按钮，“生成切片”继续作为主操作。
- 窄屏或上下堆叠布局下，点击下方候选片段的“播放预览”会自动滚到播放器位置。
- 怎么测试：重启项目后打开某个任务的 `/tasks/{task_id}/clips/review`，往下滚到第 4、第 5 条片段，点击“播放预览”，确认右侧播放器保持可见并播放对应时间段。
- 下一步：用真实任务确认播放器尺寸是否舒服；如果右侧仍觉得小，可以继续把双栏比例从约 `2:1` 调到更接近 `1.7:1`。

## 2026-06-05 本次小改动：修复抖音话题、封面和发布链路中断
- 已修复抖音发送时卡在“插入话题”步骤的 `SyntaxError: Unexpected token ')'` / `gt 不是命令`：opencli 现在不再直接走 Windows 的 `opencli.cmd` 执行复杂 JS，而是用 Node 直接运行 opencli 主入口。
- 抖音话题插入不再拼接容易被命令行拆坏的 HTML 字符串，改为在页面里创建 `data-mention="#"` 话题节点。
- AI 推荐封面选择增加文案兜底查找，会优先找“AI智能推荐封面 / 智能推荐封面 / 推荐封面”区域里的可见图片。
- 怎么测试：重启 `8002` 后打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5`，对刚才失败的抖音任务重新点“发送此条”；观察简介之后是否继续写入话题、选择 AI 推荐封面并点击底部“发布”。
- 下一步：如果这次能走到发布按钮但平台弹出验证码、风控、登录失效或人工确认，不要绕过，记录页面提示后再处理；如果仍失败，把新的完整错误发出来。

## 2026-06-05 本次小改动：先跑通抖音封面确认和发布
- 抖音话题暂时不再走蓝色话题块插入，发送时会把发送中心的 `#话题` 直接追加到作品简介里。
- AI 推荐封面点击后，如果出现“是否确认应用此封面？”弹窗，会自动点击“确定 / 确认 / 应用”，再继续点击最后的“发布”。
- 怎么测试：重启 `8002`，打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5`，重新发送刚才那条抖音任务；重点看封面弹窗是否点了“确定”，然后页面是否继续点底部“发布”。
- 下一步：如果还卡在封面区域，把当时页面截图和发送中心的新错误发出来；如果卡在自主声明或平台风控，就先按页面要求人工确认，不做绕过。

## 2026-06-06 本次小改动：修复抖音简介话题和 AI 推荐封面流程
- 抖音发送时不再插入蓝色话题块，而是把“正文 / 简介 + 平台 #话题”当作一整段简介直接复制到抖音作品描述里。
- 例如会写成：`陈亦飞回忆当年在美国陪S姐妹游玩...满满青春回忆 #小S自恋名场面 #青春回忆杀 #明星搞笑日常 #反差萌瞬间 #姐妹花趣事`。
- 抖音封面会继续使用页面里的“AI智能推荐封面”，并按“横封面 -> 竖封面 -> 完成 -> 发布”的顺序执行。
- 怎么测试：
  1. 先重启 Windows 本机 `8002` 后台服务，避免浏览器继续调用旧代码。
  2. 打开 `http://127.0.0.1:8002/publish`。
  3. 按 `Ctrl + F5` 强制刷新页面。
  4. 只选一条抖音任务，点击“发送此条”。
  5. 在抖音创作者页面检查作品描述：正文和所有 `#话题` 应该都在同一个简介框里，不再单独插入话题块。
  6. 继续观察封面弹窗：应先设置横封面，再设置竖封面，最后点击“完成”并回到发布页点击底部“发布”。
- 下一步：如果抖音弹出验证码、登录失效、风控或人工确认，请先按平台页面处理或截图发回来；项目不会绕过平台限制。

## 当前已处理

- 已新增 Docker 一键启动方式：`docker compose up --build`，固定访问 `http://127.0.0.1:8001`。
- Docker 容器内已安装 FFmpeg / FFprobe，系统状态页可用于确认视频处理依赖是否可用。
- Docker 版继续使用项目内 `data/workflow.sqlite3` 作为数据库，并把任务产物挂载到 E 盘 `E:\直播间切片工作流存储`。
- 已修复任务详情页转写完成后循环自动刷新的问题，已完成任务页面可以正常停留和编辑 AI 偏好。
- 长音频转写已改成后台分段执行：默认每 2 分钟一段、段间重叠 5 秒，完成后自动拼接到 `transcripts/transcript.md`。
- MVP 默认转写配置已改成 `medium / cpu / int8`，先保证 Windows 本地稳定跑通；CUDA 加速后续单独验证。
- 点击“生成转写 MD”后，任务详情页会自动轮询 `/api/tasks/{task_id}/transcript-status`，不需要反复手动刷新。
- 页面会展示当前转写模型、设备、计算类型和分段进度；如果进度长时间不更新，会提示可能卡住并建议重新生成。
- 已新增转写环境检查脚本和真实短音频冒烟测试脚本。
- 任务详情顶部已改为“开始处理 / 继续处理”，会自动完成“提取音频 → 转写”；生成切片只保留在片段审核页。
- 已增加完整转写原文页面，转写预览处可以直接打开完整 `transcript.md`。
- 远程 AI 失败时会暂停并写清楚原因；需要本地 Ollama 时，用户手动点击“本地 AI 分析”。
- AI 配置已修正：远程 Key 兼容 `OPENAI_API_KEY`，URL 拼接增加防呆，本地默认模型改为当前机器更容易跑通的 `qwen3:8b`。
- 已新增 `.venv\Scripts\python.exe scripts\diagnose_ai_environment.py`，用于检查远程中转站和本地 Ollama 是否真的可用。
- 本地 AI 片段审核已改为分段分析，当前 `测试2 - 19min` 已用 `qwen3:8b` 生成 5 条候选片段并进入待审核。
- 片段审核页已经可以按候选片段起止时间查看对应转写原文；后续重新运行 AI 分析时，摘要、推荐理由和剪辑建议会按更详细 Prompt 生成。

## P0：下一阶段优先做

1. 打开发送中心 `/publish`，点击“刷新发送队列”，确认已完成切片会生成抖音 + B站两条待发送任务。
2. 对任意一条队列点击“选封面帧”，确认能生成候选帧、切换封面并保存。
3. 点击“重新生成话题”，确认 AI 标题、话题和简介能补齐；如果 AI 配置不可用，应能看到可读错误。
4. 先确认 Chrome 已登录抖音创作者中心和 B站创作中心，再勾选 1 条任务测试“发送此条”。
5. 如果出现验证码、登录失效或风控弹窗，把任务状态作为失败处理，并记录错误信息，不尝试绕过平台限制。
6. 再完整跑一条真实视频流程：“上传视频 → 开始处理 → AI 分析 → 人工审核 → 自动切割 → 刷新发送队列 → 发送”。
7. 继续保留转写验证：运行 `.venv\Scripts\python.exe scripts\diagnose_transcription_environment.py`，确认当前使用的是项目虚拟环境。
8. 使用真实长视频和真实转写文本验证远程 AI / 本地 AI 的候选片段质量；如果摘要仍太短，重新运行 AI 分析让新 Prompt 生效。
9. 在 Docker Desktop 中确认项目、镜像和容器名称都显示为 `niuma-studio`，并验证容器启动、停止、端口展示和日志查看体验是否足够适合新手使用。
10. 进入字幕工作台 `/subtitles/{task_id}`，对已经出现小方块字幕的切片重新点击“自动加字幕”，确认新生成的视频中文能正常显示；旧视频不会自动批量重做。
11. 打开发送中心 `/publish`，点击“刷新发送队列”或“AI 补齐标题/话题”，确认每条待发送任务自动出现封面预览，不需要先手动点“更换封面帧”。
12. 检查发送中心的“平台 #话题”字段，确认内容是类似 `#直播切片 #高光片段` 的平台话题，而不是标题解释。
13. 用包含“屎、死、诈骗、加微信”等词的测试切片标题或摘要刷新队列，确认生成后的标题、话题和简介会自动换成更温和的表达。

## P1：随后推进

1. 用 FFprobe 读取更完整的视频编码、分辨率和音频信息。
2. 为不同内容类型增加可选 Prompt 模板，并让它与任务详情里的 AI 偏好一起进入 AI 分析流程。
3. 支持 AI 分析结果人工二次生成或局部重跑。
4. 支持打开输出目录、播放输出切片和删除失败/过期输出记录。
5. 继续优化片段预览体验：支持直接预览已生成切片、显示当前播放时间和更精确的片段区间高亮。
6. 为已隐藏任务增加恢复入口，当前隐藏不会删除任何文件。
7. 根据 `docs/PROJECT_GUIDE.md` 继续补充更适合新手的一键检查脚本。

## 暂不做

- MacBook 自动录屏。
- 自动识别直播间是否开播。
- 绕过抖音 / B站验证码、登录失效或平台风控。
- AI 生图封面和复杂封面模板。
- 多用户权限系统。

## 2026-06-07 当前配置状态

- 音频转写已配置为火山引擎远程转写，默认资源 ID 为 `volc.bigasr.auc_turbo`，音频格式为 `mp3`；2026-06-07 已更换为豆包语音新版控制台 API Key，并通过静音音频烟测。
- 文字稿分析候选切片已配置为 DeepSeek Pro：`AI_ANALYSIS_REMOTE_MODEL=deepseek-v4-pro`。
- 发送中心生成发布文案已配置为 DeepSeek Flash：`AI_PUBLISH_REMOTE_MODEL=deepseek-v4-flash`。
- Windows 本地 `8002` 后台已重启，当前服务已经能读取最新 `.env`。
- 新生成的转写文件只保留“逐句时间戳原文”；远程 DeepSeek Pro 整集一次提交逐句原文，旧文件里的分钟级重复内容会在分析时被忽略。

## 2026-06-07 下一步测试

1. 打开 `http://127.0.0.1:8002`，进入“系统状态”或相关配置入口，确认三类接口都显示为已配置。
2. 用一条真实有声视频重新跑完整流程：上传视频 -> 开始处理 -> 火山引擎生成转写；重点确认不再出现 `Invalid X-Api-Key`，并确认完整转写页只显示“逐句时间戳原文”。
3. 点击远程 AI 分析，确认 DeepSeek Pro 能整集一次分析逐句原文并生成候选切片；运行日志不应出现“本地 AI 分段分析”提示。
4. 切片生成后进入发送中心 `/publish`，点击刷新发送队列，再测试“AI 补齐标题/话题/简介”，确认 DeepSeek Flash 能生成发布文案。
5. 如果火山引擎转写失败，优先记录页面错误信息和任务日志；如果 DeepSeek 生成失败，记录失败提示和使用的是 Pro 还是 Flash。

## 2026-06-07 本次小改动：修复抖音 AI 推荐封面等待选择

- 抖音发送时会等待平台右侧固定的“AI智能推荐封面”区域生成封面，最长等待 150 秒。
- 生成后会选择最左边第一张 AI 推荐封面，并尝试点击“设为封面 / 使用封面 / 确定 / 确认 / 应用”，然后继续发布。
- 怎么测试：
  1. 重启 Windows 本地 `8002` 后台服务。
  2. 打开 `http://127.0.0.1:8002/publish`。
  3. 按 `Ctrl + F5` 强制刷新页面。
  4. 只选一条抖音任务，点击“发送此条”。
  5. 等待 1-2 分钟，看抖音页面“AI智能推荐封面”最左边图片生成后，程序是否选中它并继续点击发布。
- 下一步：如果后续出现验证码、登录失效、风控或人工确认弹窗，先按平台页面处理或截图记录；项目不会绕过平台限制。

## 2026-06-07 本次小改动：修复抖音发布按钮找不到

- 抖音发送最后一步不再只找文字严格等于“发布”的按钮，会滚动到底部并识别“发布 / 立即发布 / 确认发布 / 发布作品”。
- 同时会避开左侧菜单里的“高清发布”和右侧“发布助手”，避免点错位置。
- 怎么测试：
  1. 重启 Windows 本地 `8002` 后台服务。
  2. 打开 `http://127.0.0.1:8002/publish` 并按 `Ctrl + F5`。
  3. 只选一条抖音任务，点击“发送此条”。
  4. 等 AI 推荐封面生成并设置后，看页面是否继续滚动到底部并点击最终发布按钮。
- 下一步：如果还失败，优先看新错误是 `douyin_cover_confirm_not_found` 还是 `douyin_publish_button_not_found:...`，后者冒号后会带上当时页面看到的按钮文字，方便继续定位。

## 2026-06-07 本次小改动：避免抖音假发送和简介重影

- 抖音发送现在不再“点到发布按钮就算成功”，必须等到平台提示“发布成功 / 已提交审核 / 审核中 / 投稿成功”，或作品管理里出现对应标题，才会标记为已发布。
- 如果发布后没有成功信号，会返回 `douyin_publish_not_confirmed`；如果遇到验证码、登录失效或风控，会返回 `douyin_publish_blocked`。
- 简介填写会优先找“作品描述 / 简介 / 描述”区域，并检查重复写入，减少抖音简介内容重影。
- 怎么测试：
  1. 重启 Windows 本地 `8002` 后台服务。
  2. 打开 `http://127.0.0.1:8002/publish` 并按 `Ctrl + F5`。
  3. 只选一条抖音任务，点击“发送此条”。
  4. 观察简介框是否只有一份正文和话题，AI 推荐封面是否真正设为封面。
  5. 发布后等待页面出现成功 / 审核中提示，再去作品管理确认是否有对应标题。
- 下一步：如果失败，请把新的红色错误完整发回来；这次错误里会明确区分“没确认发布成功”和“平台验证码 / 风控 / 登录问题”。

## 2026-07-27 本次功能：全自动封面与一键补齐

- 新建的全自动任务会让 AI 为每条候选短视频返回 `cover_time_seconds`，完成切片后自动从对应秒数生成独立 JPG 封面，并同时写入抖音和 B站发布任务。
- 旧任务没有 AI 封面秒数时，发送中心的“一键补充所有封面”会从短视频中间 50% 位置截帧；同一切片的双平台任务只生成一次。
- 按钮只补尚未发布且封面为空的任务，不覆盖人工已经生成或更换的封面，也不改 MP4 视频内容。
- 下一步手动验收：
  1. 重启本地后台后打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5` 强制刷新。
  2. 在“内容准备”标题右侧确认能看到“一键补充所有封面（数量）”。
  3. 点击一次并等待完成提示；当前历史数据预计补齐 29 条发布任务，对应约 20 张不同切片封面。
  4. 抽查同一切片的抖音和 B站卡片，确认两边都显示封面；已经有封面的卡片应保持不变。
  5. 新建一条全自动测试任务，完成后直接进入发送中心，确认不再显示“缺少：封面”。
- 如果个别切片显示失败，请保留页面完整错误信息；批量流程会继续处理其他切片，不需要逐条重新点击。

## 2026-07-27 跨午夜排期与新任务默认值验收

1. 打开 `http://127.0.0.1:8001/publish`，按 `Ctrl + F5` 强制刷新，再进入“排期计划”。
2. 选择 10 条同平台任务，点击“设置排期”，把第 1 条设为次日 06:00、间隔设为 3 小时、每日时段设为 06:00 → 00:00。
3. 点击“预览排期”，应依次看到当天 06、09、12、15、18、21 点，次日 00、06、09、12 点；`00:00` 不应再报错。
4. 修改任一时间后，“确认应用具体时间”应立即禁用；重新预览成功后才能确认，避免保存旧时间。
5. 打开“新建任务”，确认“单条切片最长”为 10 分钟、“候选片段数量”为 12 条；这两个值只影响新任务，不会修改历史任务。
6. 本次验收只需检查预览和新建表单，不需要点击“立即发送”，不会触发抖音或 B站真实投稿。

## 2026-07-27 取消发送返回内容准备验收

1. 重启本地后台后打开 `http://127.0.0.1:8001/publish`，按 `Ctrl + F5` 强制刷新。
2. 在“排期计划”选择一条测试任务，点击“取消发送并返回准备”并确认。
3. 页面应自动切换到“内容准备”，对应视频、标题、简介、话题和封面都应保留；排期时间应变为“未排期”。
4. 之前由旧版“取消任务”产生、错误信息为“用户取消任务”的记录，会在数据库初始化时自动恢复；如果同一切片和平台已经存在新的活跃任务，则保留新的任务且不重复恢复。
5. 继续确认“移出内容准备”仍会隐藏任务，并可在执行记录中恢复；“跳过任务”仍保持终止状态，不会自动回到准备区。
6. 本次验收不要点击“立即发送”，不会触发抖音或 B站真实投稿。

## 2026-08-02 E 盘存储与永久删除验收

1. 重启本地后台，打开“系统状态”，确认“视频临时与导出目录”显示“E 盘就绪”，路径分别是 `_临时上传` 和 `_发布包`。
2. 新建一个测试任务并上传视频，确认任务原片和后续切片只出现在 `E:\直播间切片工作流存储\任务名` 下；C 盘项目目录不应出现新的生产视频副本。
3. 在任务列表点击“永久删除”，确认提示明确说明无法恢复且外部原片保留；删除后对应 E 盘任务目录和发布包应消失。
4. 对 NAS 或 E 盘其他目录的引用任务执行删除时，只删除任务生成物，外部唯一原片必须仍然存在。
5. 转写、切片或真实发布进行中时，删除应被拒绝并显示原因；等待任务结束后再删除。
6. `scripts/purge_deleted_task_media.py` 默认只输出清单，只有显式带 `--apply` 才会永久清理，并在 `data/backups` 留下 SQLite 元数据备份。

## 2026-08-02 任务进度自动刷新验收

1. 重启本地后台，打开“新建任务”，保持“全自动流程”开启并创建任务。
2. 创建成功后停留在任务详情页，不需要按 `F5`；“状态概览”和右侧“运行日志”应每 3 秒自动更新。
3. 观察蓝色当前节点依次向后移动，已经完成的节点变绿；如果某一步失败，对应节点变黄，并自动出现“重试全自动流程”。
4. 流程完成后，进度应显示 100%，顶部自动出现“前往发送中心”，有候选片段时同时出现“检查候选片段”。
5. 可以在 AI Prompt 文本框临时输入几个字后继续等待，确认状态更新时文字没有消失、页面也没有跳回顶部。
6. 如果网络短暂中断，状态卡会显示“自动更新暂时中断，正在重试”；恢复后不需要手动刷新。

## 2026-08-11 当前发布计划

1. 保持电脑、Docker Desktop 和网络开启，至少持续到最后一条计划时间 2026-08-13 10:00（北京时间）；不要在排期中途关闭工作台或 Windows 发布 Worker。
2. 2026-08-11 计划 6 条：07:00、10:00、13:00、16:00、19:00、22:00。
3. 2026-08-12 计划 6 条：07:00、10:00、13:00、16:00、19:00、22:00。
4. 2026-08-13 计划 2 条：07:00、10:00。
5. 发送中心已保持打开，可在“排期计划”查看月历；若出现验证码、登录失效或平台风控，请按页面提示人工处理，不要重复点击“立即发送”。
6. 当前最新确认成功发布时间为 2026-08-11 01:49:08；这是 Docker Desktop 自动恢复后处理的第 3 条过期任务，已经从剩余排期中排除。

## 2026-08-16 Windows 启动检查方法

1. 双击 `C:\Users\10578\Documents\启动牛马片场.bat`，确认不再出现 `UnexpectedToken`、乱码或缺少右大括号等 PowerShell 解析错误。
2. 等待脚本显示启动成功，并确认 Docker Desktop 中 `niuma-studio` 容器状态为 `healthy`。
3. 使用 `http://127.0.0.1:8001/` 打开牛马片场；不要继续使用已经停用的 `http://localhost:6866/`。
4. 若发送中心提示登录失效、验证码或平台风控，按页面提示人工处理，不要绕过验证或重复投稿。
5. 如果以后启动时只出现 Docker Hub 连接超时，并且本机已有 `niuma-studio:latest` 镜像，可在项目目录运行 `powershell.exe -ExecutionPolicy Bypass -File .\scripts\start.ps1 -NoBuild`，使用本地镜像启动。

## 2026-08-22 Codex CLI 验收

1. 重启牛马片场后打开系统状态页，确认默认分析方式和发送中心文案方式均为“Codex CLI”，状态显示 CLI 可执行。
2. 先用一条短测试文字稿执行 Codex 分析，核对候选片段 JSON、时间范围和封面时间；这一步会消耗 Codex 额度，需人工发起。
3. 在发送中心只对一条未发布草稿点击 AI 补齐，确认标题、话题、简介通过现有内容安全规则；不要点击真实发布。
4. 若 Codex 不可用，可在系统状态页手动选择远程 AI 或本地模型；旧 API 配置和历史任务保持不变。

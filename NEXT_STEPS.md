# Next Steps

## 当前已处理

- 长音频转写已改成后台分段执行：默认每 2 分钟一段、段间重叠 5 秒，完成后自动拼接到 `transcripts/transcript.md`。
- MVP 默认转写配置已改成 `medium / cpu / int8`，先保证 Windows 本地稳定跑通；CUDA 加速后续单独验证。
- 点击“生成转写 MD”后，任务详情页会自动轮询 `/api/tasks/{task_id}/transcript-status`，不需要反复手动刷新。
- 页面会展示当前转写模型、设备、计算类型和分段进度；如果进度长时间不更新，会提示可能卡住并建议重新生成。
- 已新增转写环境检查脚本和真实短音频冒烟测试脚本。
- 任务详情顶部已改为“开始处理 / 继续处理”，会自动完成“提取音频 → 转写”；生成切片只保留在片段审核页。
- 已增加完整转写原文页面，转写预览处可以直接打开完整 `transcript.md`。
- 远程 AI 失败时会自动尝试本地 AI，并把两边错误写清楚。
- AI 配置已修正：远程 Key 兼容 `OPENAI_API_KEY`，URL 拼接增加防呆，本地默认模型改为当前机器更容易跑通的 `qwen3:8b`。
- 已新增 `.venv\Scripts\python.exe scripts\diagnose_ai_environment.py`，用于检查远程中转站和本地 Ollama 是否真的可用。
- 本地 AI 片段审核已改为分段分析，当前 `测试2 - 19min` 已用 `qwen3:8b` 生成 5 条候选片段并进入待审核。

## P0：下一阶段优先做

1. 先运行 `.venv\Scripts\python.exe scripts\diagnose_transcription_environment.py`，确认当前使用的是项目虚拟环境。
2. 用真实任务音频运行 `.venv\Scripts\python.exe scripts\test_real_transcription_smoke.py "音频路径"`，确认 20 秒短音频能真实转写成功。
3. 回到任务详情页，对 `测试2 - 19min` 点击“开始处理”或“继续处理”，确认只点一次即可自动提取音频并转写。
4. 转写完成后打开“查看完整原文”，确认完整文本可读。
5. 打开 `测试2 - 19min` 的片段审核页，检查本地 AI 生成的 5 条候选片段是否符合预期。
6. 保存候选片段后，在片段审核页点击“生成切片”，验证 FFmpeg 能按启用片段输出短视频。
7. 先把重新生成后的远程中转站新 Key 写入 `.env` 的 `AI_REMOTE_API_KEY`，或者写入 `OPENAI_API_KEY`。
8. 运行 `.venv\Scripts\python.exe scripts\diagnose_ai_environment.py 37601f8548fd`，确认本地 AI 通过；远程如果仍失败，重点检查 Key 和中转站账号权限。
9. 用一条真实视频完整跑通“上传视频 → 开始处理 → AI 分析 → 人工审核 → 自动切割”。
10. 使用真实长视频和真实转写文本验证远程 AI / 本地 AI 的候选片段质量。
11. 接入真实视频预览和片段区间预览。
12. 增加一个一键启动脚本，固定使用当前实际运行端口，避免误开旧服务端口。

## P1：随后推进

1. 用 FFprobe 读取更完整的视频编码、分辨率和音频信息。
2. 为不同内容类型增加可选 Prompt 模板，并让它与任务详情里的 AI 偏好一起进入 AI 分析流程。
3. 支持 AI 分析结果人工二次生成或局部重跑。
4. 支持打开输出目录、播放输出切片和删除失败/过期输出记录。
5. 为已隐藏任务增加恢复入口，当前隐藏不会删除任何文件。
6. 根据 `docs/PROJECT_GUIDE.md` 继续补充更适合新手的一键检查脚本。

## 暂不做

- MacBook 自动录屏。
- 自动识别直播间是否开播。
- 自动发布到抖音 / B站。
- 自动生成封面。
- 多用户权限系统。

# Next Steps

## 2026-06-04 AI 置信度分数修复后怎么测试
1. 先重启本地后端服务，确保运行的是这次修复后的代码。
2. 打开刚才报错的任务详情页。
3. 在 AI 分析区域重新点击远程 AI 分析，等待右侧运行日志刷新。
4. 正常情况：不会再因为 `confidence_score` 显示 8.9、7.8 这类分数而报 `Input should be less than or equal to 1`，而是进入候选片段生成和审核流程。
5. 如果仍然失败，把任务详情页右侧运行日志最后 20 行发给我；这时优先判断是否还有别的字段格式、时间范围或接口输出截断问题。

## AI 置信度分数修复后下一步我建议你做什么
1. 先用同一个失败任务重新跑一次远程 AI 分析，不需要新建任务。
2. 跑成功后进入片段审核页，确认候选片段的标题、时间、摘要和置信度都能正常显示。
3. 如果候选片段质量可以，再点击生成切片，检查输出短视频能否正常播放。

## 2026-06-04 运行日志精简后怎么测试
1. 打开或刷新任务详情页：`http://localhost:8001/tasks/fba336c5523b`。
2. 看页面右侧，确认标题只显示“运行日志”，不再出现“日志 / 元信息”和一长串源视频路径、任务目录、分析文件路径。
3. 在 AI 分析区域点击一次“DeepSeek AI 分析”或“本地 AI 分析”，确认右侧运行日志仍然会刷新。
4. 如果你只是检查页面外观，不想真的重跑 AI，可以先只刷新页面确认右侧结构已经变简单。

## 运行日志精简后下一步我建议你做什么
1. 先确认右侧日志区域是不是比之前清爽，尤其是长视频路径不再挤占页面。
2. 如果以后还想看源视频路径、任务目录这些信息，下一步可以做一个“任务文件”小入口，点击后再展开，不默认占用日志区域。

## 2026-06-04 浏览器标签页和收藏夹 logo 怎么测试
1. 先重启或等待本地后台自动刷新，确保后端加载到新的 `/favicon.ico` 路由。
2. 打开 `http://127.0.0.1:8001`，看浏览器标签页标题左边是否出现牛马小图标。
3. 如果 Chrome 还显示旧图标，先按 `Ctrl + F5` 强制刷新；收藏夹图标有缓存，必要时删除旧收藏后重新收藏一次。
4. 直接打开 `http://127.0.0.1:8001/favicon.ico`，正常情况会看到牛马图标或浏览器直接下载/展示这个图标文件。

## 浏览器图标更新后下一步我建议你做什么
1. 先确认标签页小图标是否够清楚，尤其是 16x16 很小的时候能不能看出是牛马头像。
2. 如果太糊或太复杂，下一步可以单独做一个更简化的“小尺寸专用 favicon”，不影响左侧导航的大 logo。

## 2026-06-04 牛马片场品牌更新后怎么测试
1. 先重启本地后台服务，确保页面加载的是新配置和新静态资源。
2. 打开 `http://127.0.0.1:8001`，看左上角是否显示牛马吉祥物 logo、`牛马片场` 和 `本地 AI 高光生产后台`。
3. 如果用 Docker 启动，打开 Docker Desktop，确认项目、镜像和容器名称显示为 `niuma-studio`。
4. 打开 `README.md` 和 `docs/PROJECT_GUIDE.md`，确认当前项目名已经是“牛马片场 / NiuMa Studio”。
5. 随便打开一个旧任务详情页，确认旧视频、转写、AI 分析和切片路径没有因为品牌更新而丢失。

## 牛马片场品牌更新后下一步我建议你做什么
1. 先只检查首页左上角品牌区，不急着跑完整视频流程。
2. 如果 logo 在你的屏幕上显得太可爱、太大或太小，下一步优先微调 logo 尺寸和侧边栏间距。
3. 品牌确认满意后，再决定是否把 E 盘历史目录 `E:\直播间切片工作流存储` 安全迁移为新名字；这一步要单独做备份和迁移计划。

## 2026-06-04 AI 远程分析旧字段修复后怎么测试
1. 先重启本地后端服务，确保运行的是这次修复后的代码。
2. 打开之前出现 `AI 返回非法 JSON` 报错的任务详情页。
3. 在 AI 分析区域再次点击远程 AI 分析，等待右侧运行日志刷新。
4. 正常情况：页面不再提示 `clip_id` 或 `spread_value` 字段缺失，而是生成候选片段，并可以进入片段审核页。
5. 如果仍然失败，把任务详情页右侧运行日志最后 20 行发给我；下一步优先判断是 AI 输出被截断、时间范围不合规，还是模型接口本身返回异常。

## AI 远程分析旧字段修复后下一步我建议你做什么
1. 先用同一个报错任务重新跑一次远程 AI 分析，确认这次能生成候选片段。
2. 跑成功后进入片段审核页，先检查标题、摘要、传播价值和剪辑建议是否都能正常显示。
3. 如果候选质量可以，再点击生成切片，检查输出短视频能否正常播放。
4. 如果候选片段内容仍然不理想，下一步再微调 Prompt；这次先确认 JSON 字段兼容问题已经解决。

## 2026-06-02 DeepSeek AI JSON 修复后怎么测试
1. 先重启本地后台服务，确保运行的是这次修复后的代码。
2. 打开刚才报错的任务详情页，进入“AI 分析”板块。
3. 确认当前 Prompt 方案仍然选择 2 号“康熙来了综艺短视频切片专家”。
4. 点击“DeepSeek AI 分析”，等待右侧运行日志刷新。
5. 正常情况：页面不再停在 `AI 返回非法 JSON，安全重试后仍失败`，而是生成候选片段摘要，并可以点击进入片段审核页。
6. 如果仍失败，把任务详情页右侧运行日志最后 20 行发给我；这时优先判断是输出被截断、字段缺失、时间范围不合规，还是 DeepSeek 账号 / 模型权限问题。

## DeepSeek AI JSON 修复后下一步我建议你做什么
1. 先用同一个《康熙来了》任务重新跑一次 DeepSeek AI 分析，确认 2 号 Prompt 可以稳定生成候选片段。
2. 跑成功后进入片段审核页，不急着手动改，先看候选片段是否包含铺垫、爆点、反应和自然收尾。
3. 如果候选质量可以，再点击“生成切片”，检查输出短视频是否能正常播放。
4. 如果候选片段仍然太短或边界不自然，下一轮优先微调 2 号 Prompt 的片段边界要求，而不是继续改 JSON 解析。

## 2026-06-02 停止转写卡住怎么测试
1. 先重启本地后台服务，确保运行的是这次修复后的代码。
2. 打开当前卡住的任务详情页：`http://localhost:8001/tasks/fba336c5523b`。
3. 刷新页面一次，等待 5 秒左右；如果之前只是遗留的“正在停止”状态，页面应自动变成“转写已停止，可以重新生成转写”。
4. 如果页面上还有“停止转写”按钮，再点一次；这次不应该继续卡住，应提示当前没有正在运行的转写任务，或显示已停止。
5. 确认任务状态不再显示“转写中”后，再按需要点击“重新生成转写”重新跑一次。

## 停止转写卡住修复后下一步我建议你做什么
1. 先用当前任务确认页面能从“正在停止”恢复出来。
2. 再新建一个短视频测试任务，开始转写后立刻点“停止转写”，确认它能在当前小段结束后变成已停止。
3. 如果长视频转写仍然很慢，下一步优先检查火山引擎转写配置和分段时长，不要反复刷新页面。

## 2026-05-30 火山引擎远程转写怎么测试
1. 重启本地后台服务，避免旧代码还在运行。
2. 打开任务“测试4”的详情页。
3. 点击“重新 AI 转写（火山引擎）”或页面里对应的远程转写按钮。
4. 看右侧日志：如果出现“正在请求火山引擎转写第 1/23 段”，后面继续出现“火山引擎已完成第 N/23 段”，说明远程转写已经恢复。
5. 等进度到 100%，确认页面生成了 `transcripts/transcript.md`，任务状态进入“待 AI 分析”。
6. 如果前面几秒是静音，日志短暂停留是正常的；程序现在会跳过无有效人声的小段，不会因为静音直接失败。

## 火山引擎远程转写下一步我建议你做什么
1. 先用“测试4”完整跑一次远程转写，不要中途点停止。
2. 转写完成后，再点击 DeepSeek AI 分析生成候选片段。
3. 如果远程转写仍报错，把任务详情页右侧日志最后 20 行发给我，我继续帮你定位是额度、密钥权限、音频格式，还是接口返回的新错误。

## 2026-05-30 分支合并后怎么检查
1. 当前已经在主分支 `master`，并且 `codex/subtitlefunction` 的内容已经合并进来。
2. 你可以在项目继续开发前，先打开常用页面做一次简单确认：任务列表、任务详情、字幕工作台、片段审核页。
3. 如果页面能正常打开，说明这次分支合并没有破坏本地后台。

## 分支合并后下一步我建议你做什么
1. 先不要删除 `codex/subtitlefunction` 分支；确认主分支运行正常后再决定是否清理。
2. 下一轮开发建议直接从 `master` 新建一个新的功能分支，避免继续在旧功能分支上叠加修改。

## 2026-05-27 项目文件夹改名与回收站怎么测试
1. 打开 Windows 文件资源管理器，进入 `E:\直播间切片工作流存储`。
2. 确认根目录现在能看到 `测试2 - 19min`、`测试3 - 康熙来了20160104` 和 `_回收站`，不再是那几个看不懂的短 ID 文件夹。
3. 打开 `_回收站`，确认里面有 `测试1` 和 `闭环验证任务`；这两个是之前页面里已经隐藏的项目，文件没有删除。
4. 重启本地后台后，打开 `http://127.0.0.1:8001/tasks`，确认任务列表里还能看到正常项目。
5. 点进 `测试2 - 19min` 或 `测试3 - 康熙来了20160104`，确认详情页能打开，视频、切片、字幕路径不报错。
6. 新建一个小测试项目，项目名随便填一个容易认的名字，上传一个短视频；创建成功后去 E 盘确认新文件夹直接叫这个项目名。
7. 在任务列表点击“移入回收站”，确认网页列表里项目消失，同时 E 盘对应文件夹进入 `_回收站`。

## 项目文件夹改名下一步我建议你做什么
1. 先用现有两个正常项目各打开一次详情页和切片预览，确认旧视频还能播放。
2. 再新建一个很小的测试项目验证新规则；确认无误后，你以后就可以直接按项目名去 E 盘找完成视频。
3. `_回收站` 里的内容系统不会自动删除，你确认不需要后再手动删。

## 2026-05-27 片段审核功能优化怎么测试
1. 重启本地服务后，打开一个已经生成 AI 候选片段的任务，进入 `/tasks/{task_id}/clips/review`。
2. 找到一条你不想要的候选片段，点击卡片右上方的“删除”，确认这张卡片会立刻从页面消失。
3. 刷新片段审核页，确认刚才删除的片段不会重新出现。
4. 继续点击“生成切片”，确认系统只会使用剩下的、仍然启用的候选片段。
5. 连续向下浏览多条候选片段，确认卡片比之前更紧凑，推荐理由和剪辑建议需要展开后再看。

## 片段审核功能优化下一步我建议你做什么
1. 先用一个测试任务删除 1 条明显不合适的 AI 候选片段，确认刷新后仍然隐藏。
2. 如果你希望误删后可以恢复，下一轮可以增加“显示已删除 / 恢复片段”入口；当前版本先按你的要求做成删除后不占页面位置。

## 2026-05-27 AI 片段完整性怎么验收
1. 重启本地服务后，打开任意任务详情页，进入“AI 分析”板块。
2. 切换到 `2 号` 或 `3 号` Prompt，确认其中一个名称是“综艺访谈完整上下文专家”；如果你之前编辑过 2 号方案，系统不会覆盖它，会把新版 Prompt 放到空的 3 号方案。
3. 新建综艺或访谈任务时，把“单条切片最长”保持默认 `5 分钟`，或按页面提示设置为 `4-6 分钟`。
4. 对同一条《康熙来了》视频分别用旧 Prompt 和“综艺访谈完整上下文专家”Prompt 跑 AI 分析，对比候选片段是否更完整地包含铺垫、爆点、反应和收尾。
5. 进入片段审核页，不先手动补剪，直接点击“生成切片”，检查输出视频是否明显减少前后文割裂感。
6. 如果日志提示某些 AI 片段短于 `45 秒`，先不要急着手动修，优先用“综艺访谈完整上下文专家”Prompt 或把单条最长调到 `4-6 分钟` 后重跑 AI。

## AI 片段完整性下一步我建议你做什么
1. 先固定用“综艺访谈完整上下文专家”Prompt 跑 3 条不同类型视频：一条直播干货、一条综艺、一条访谈，记录哪类仍然容易短。
2. 如果 2 号 Prompt 稳定后，下一轮再做“AI 分析完成后自动生成切片”的开关，让审核页逐步变成只回看不必操作。

## 2026-05-26 字幕页片段内剪切怎么验收
1. 打开 `http://localhost:8001/subtitles/f8e1edf6a57f`，进入字幕推送任务页。
2. 点击任意已生成短视频卡片里的“修改剪切”，确认弹窗播放的是这个短视频片段本身，而不是整段源视频。
3. 拖动黄色左右把手，点击“预览这一段”，确认视频只在选区内播放。
4. 点击“保存入点 / 出点”，再回到片段审核页确认该候选片段的起止时间已更新。
5. 在片段审核页重新生成切片，检查新输出视频是否只保留这次选中的片段范围。

## 字幕页片段内剪切下一步我建议你做什么
1. 先用一条 1 分钟左右的短片段试一次，只把开头或结尾裁掉 2-3 秒，确认保存后的审核页时间符合预期。
2. 如果你希望更像手机剪辑软件，下一轮可以继续补真实缩略图胶片；当前版本先把“只裁当前片段”和“保存时间换算正确”稳定下来。

## 2026-05-26 片段审核 noUiSlider 源监视器怎么验收
1. 打开 `http://localhost:8001/tasks/f8e1edf6a57f/clips/review`，刷新页面。
2. 点击任意候选片段的“编辑出入点”，确认弹窗下方时间轴变成更顺手的双手柄蓝色范围滑块。
3. 拖动左手柄和右手柄，确认入点、出点和“片段时长”会实时变化。
4. 点击 `1秒 / 5秒 / 15秒` 步长按钮，再点“入点提前 / 后移”或“出点提前 / 后移”，确认每次按当前步长调整。
5. 点击“设为入点 / 设为出点”，确认滑块会跟随视频当前播放位置。
6. 点击“预览当前片段”，确认视频从入点播放，到出点附近自动暂停。
7. 点击“应用到片段”，确认卡片里的开始时间和结束时间已更新；再点击“保存修改”，刷新后确认时间仍保留。

## 源监视器下一步我建议你做什么
1. 先挑一条 AI 截得不准的片段，只改这一条，确认拖动手感是否比之前自然。
2. 如果你觉得仍然不够像剪辑软件，下一步再考虑加缩略图胶片或键盘快捷键；当前版本先把“能精确拖入点 / 出点”稳定下来。

## 2026-05-25 抖音 + B站发布后台怎么验收
1. 打开 `http://127.0.0.1:8001/publish`，确认左侧导航有“发布中心”，页面能看到“抖音后台”和“B站后台”两个页签。
2. 先在抖音后台填写 Client Key、Client Secret、回调地址和接口地址，点击“保存配置”，再点击“测试配置”。
3. 没有真实开放平台账号前，可以先新增一个“手动账号”；如果要真实发布，必须先完成抖音授权并拿到 access_token / open_id。
4. 找到一条待发布切片，先点“保存草稿”或“创建人工发布任务”，确认下方发布记录出现新任务。
5. 不配置账号时点击“发布到抖音”，应该提示缺少账号或配置，不应该新增错误发布任务。
6. B站后台现在用于保存开放平台配置、账号和稿件字段；未拿到 B站开放平台投稿接口权限前，不要期待它真实上传成功。

## 发布后台下一步我建议你做什么
1. 先去抖音开放平台创建应用，把本地回调地址设置成 `http://127.0.0.1:8001/api/publish/douyin/oauth/callback`。
2. 拿到 Client Key / Client Secret 后填到发布中心，优先用一条很短、不重要的视频做真实发布测试。
3. B站这边先完成开放平台入驻和投稿接口权限申请，拿到官方接口地址、签名规则和分区 ID 后，再补齐真实 provider 调用。
4. 每次真实发布前只选一条切片测试，确认标题、标签、账号和视频版本都正确，再考虑批量任务。

## 2026-05-25 字幕工作台剪切弹窗怎么验收
1. 打开 `http://localhost:8001/subtitles/f8e1edf6a57f`，刷新页面。
2. 找到任意一条切片，点击“修改剪切”。
3. 确认弹窗里是源视频预览，下方时间轴像手机剪辑一样：左侧播放按钮、黑色帧条、粉色外框、黄色选区和左右两个黄色手柄。
4. 拖动黄色左手柄调整入点，拖动黄色右手柄调整出点；也可以直接改“入点 / 出点”输入框。
5. 点击“预览这一段”，确认视频会从入点播放，到出点附近自动停住。
6. 点击“保存入点 / 出点”，看到“已保存”的提示。
7. 点击“去片段审核重新切片”，进入片段审核页后点击“生成切片”；这一步完成后，字幕工作台里才会出现按新时间切出来的视频。

## 下一步我建议你做什么
1. 先只拿一条你不满意的切片测试，不要一次改很多条。
2. 调好入点 / 出点并保存后，务必回片段审核页重新生成切片。
3. 重新生成后再回字幕工作台，播放新切片确认开头和结尾是否对。

## 2026-05-25 片段审核源监视器怎么验收
1. 打开片段审核页 `/tasks/f8e1edf6a57f/clips/review`，刷新页面。
2. 点击任意候选片段的“编辑出入点”，确认会打开深色源监视器弹窗。
3. 点击播放，在合适位置分别点“设为入点”和“设为出点”，确认蓝色范围和时间码会变化。
4. 拖动蓝色范围左右手柄，确认入点 / 出点可以快速调整。
5. 点击“预览当前片段”，确认视频从入点播放，到出点附近自动暂停。
6. 点击“应用到片段”，确认卡片里的开始时间和结束时间已更新；再点击“保存修改”，刷新后确认时间仍保留。
7. 调整后点击“查看这一段转写”，确认读取的是当前页面上的新时间范围。

## 2026-05-25 运行日志浅色化后怎么验收
1. 打开任务详情页 `/tasks/f8e1edf6a57f`，刷新页面。
2. 看右侧“运行日志”，确认它已经不是黑底，而是浅色蓝白卡片。
3. 如果日志很多，拖动日志框内部滚动条，确认文字清晰、滚动正常。
4. 后续再点击 DeepSeek AI 分析或本地 AI 分析时，继续观察这个浅色日志框是否能持续刷新。

## 2026-05-24 AI 分析日志和进度条后
- 任务详情页现在可以边跑 AI 分析边看右侧运行日志，不需要再单独打开 `process.log` 文件。
- AI 分析按钮下方会显示进度条；长视频分析时如果停在“分段生成候选片段”，属于正常等待。
- 如果 AI 分析失败，先看页面右侧运行日志最后几行，再决定是检查 DeepSeek Key、余额、网络，还是改用本地 AI。

## AI 分析日志下一步优先测试
1. 打开一个已经完成转写的任务详情页。
2. 选择 Prompt 方案后点击“DeepSeek AI 分析”或“本地 AI 分析”。
3. 确认按钮下方出现进度条，右侧“运行日志”持续刷新。
4. 分析完成后进入片段审核页，检查候选片段是否正常出现。

## 2026-05-24 长视频 AI 分析修复后
- 已把远程 DeepSeek 和本地 Ollama 都改成分段分析，默认每段约 3 分钟，适合继续验证 40 分钟和 1 小时以上的长视频。
- 已兼容旧 Prompt 返回的 `clip_key`、`viral_value` 等字段，当前 2 号“康熙来了综艺短视频切片专家”Prompt 也已更新为新 JSON 格式。
- 页面上 AI 失败提示会变短；如果还失败，优先看任务右侧日志或 `E:\直播间切片工作流存储\任务ID\logs\process.log`。

## 长视频 AI 下一步优先测试
1. 打开任务 `f8e1edf6a57f`，先点击“DeepSeek AI 分析”，等待它按分段生成候选片段。
2. 成功后进入片段审核页，检查候选片段的标题、起止时间、推荐理由和传播价值是否合理。
3. 再点击“本地 AI 分析”做对比；本地 Ollama 会更慢，但不应再因为 `clip_key` 或 `spread_value` 字段失败。
4. 如果 40 分钟任务稳定，再上传或选择 1 小时以上视频，按同样流程验证转写完成后的 AI 分析。

## 2026-05-24 国内远程转写接入后
- 已把转写默认方案改为“火山引擎远程转写优先，本地 faster-whisper 兜底”。
- 已新增 `.env` 配置项：`TRANSCRIPTION_PROVIDER=volcengine`、`TRANSCRIPTION_FALLBACK_PROVIDER=local`、`VOLCENGINE_ASR_API_KEY`、`VOLCENGINE_ASR_RESOURCE_ID=volc.bigasr.auc_turbo`。
- 已新增火山引擎连通性测试脚本：`scripts/test_volcengine_transcription_connection.py`。
- 已新增“停止转写”按钮；如果误用本地模型开始转写，可以先停止，再重启服务后重新生成。
- 下一步先在火山引擎控制台开通豆包语音相关权限，拿到 App Key 后填入 `.env` 的 `VOLCENGINE_ASR_API_KEY`。

## 国内转写下一步优先做
1. 用真实任务音频运行 `.venv\Scripts\python.exe scripts\test_volcengine_transcription_connection.py "音频路径"`，确认火山引擎能返回中文转写。
2. 修改 `.env` 后先重启 FastAPI / Docker 服务，再对 40 分钟视频点击“重新生成转写”，观察速度是否明显快于本地 CPU。
3. 如果火山引擎 Key、余额或权限异常，先看页面错误；系统会按配置尝试退回本地转写。
4. 后续再按同一 Provider 接口接阿里云、腾讯云或讯飞，实现真正多家切换。

## 2026-05-24 自动加字幕功能完善后
- 已把 `/subtitles` 改成字幕任务列表；不同视频任务需要点进详情页 `/subtitles/{task_id}` 后分别处理。
- 已新增字幕样式表 `subtitle_style_presets` 和字幕任务表 `subtitle_jobs`，样式会保存到 SQLite。
- 已接入 FFmpeg 自动加字幕：生成 `.ass` 字幕文件和 `06_subtitled` 目录下的带字幕 MP4。
- 已为“修改剪切”增加弹窗预览，方便先看画面和拖动时间轴；实际保存剪切时间仍回片段审核页。

## 下一步优先做
1. 字幕批量处理：在单任务字幕页增加“一键处理本任务全部待加字幕切片”。
2. 字幕编辑：允许在生成前查看和修改每条切片抽取出的字幕文本。
3. 剪切弹窗保存：把弹窗里的起止时间滑块真正写回候选片段，再触发重新切片。
4. 打码区域：先做固定区域打码并保存到数据库，再接 FFmpeg filter。
5. 发布前确认：带字幕成片生成后，进入抖音 / B站标题、简介、标签和二次确认页面。

## 2026-05-24 v1.1 清理与呈现优化
- 已将项目口径更新为 `v1.1`：本地上传、转写、AI 分析、人工审核、自动切片全流程已走通。
- 已清理重复 UI 参考图根目录副本，保留正式文件 `docs/design/live_streaming_slicing_workflow_ui_16x9.png`。
- 已移除旧的切片占位接口，统一使用真实切割接口 `POST /api/tasks/{task_id}/process/cuts`。
- 已优化工作台、任务列表和字幕推送页，让“输出切片、待加字幕、待推送”更直观。
- 已修复 `docs/DATABASE_SCHEMA.md` 乱码，并同步当前真实表结构。

## v1.1 下一步优先做
1. 字幕添加：新增 `subtitle_jobs` 和 `subtitle_style_presets`，把字幕样式从浏览器本地保存改为保存到 SQLite。
2. 字幕渲染：从转写文本里按切片时间范围提取字幕，接入 FFmpeg 生成带字幕视频。
3. 字幕预览：在 `/subtitles` 展示“原切片 / 带字幕成片”对比，保留人工复核。
4. 一键推送：先做发布任务表和发布前确认页，再接抖音 / B站账号与平台接口。
5. 安全边界：自动发布必须保留二次确认、日志记录、失败重试和不明文保存账号密码。

## 2026-05-23 本次小改动：新增字幕、推送功能 demo
- 已新增“字幕推送”页面，切片生成后的短视频会进入这里作为待加字幕队列。
- 已预留视频预览、删除、返回修改剪切、自动加字幕、字幕样式、打码和抖音 / B站一键推送入口。
- 已新增计划书 `docs/SUBTITLE_AND_PUBLISH_PLAN.md`，后续按“字幕渲染 → 打码 → 发布任务”的顺序逐步接真实能力。
- 下一步建议先建 `subtitle_jobs` 和字幕样式表，把当前浏览器本地保存的字幕样式改为保存到 SQLite，然后接入 FFmpeg 生成带字幕视频。

## 2026-05-23 本次小改动：AI 分析预览和数量调整
- 已把 AI 分析预览改为可刷新保留，任务详情页会显示最近一次分析摘要。
- 已新增 AI 分析历史结果面板，可按“第 N 次分析”查看历史，并可恢复整次历史结果到片段审核。
- 已在 AI 分析区域新增候选片段数量输入框，默认 5 条，分析前可改为 1-50 条。
- 下一步用同一任务分别生成 3 条和 5 条结果，确认历史恢复后片段审核页候选内容同步变化。

## 2026-05-23 本次小改动：AI 分析 UI 与审核来源提示
- 已把任务详情页 AI Prompt 方案改成 1、2、3 号标签页，切换标签后只显示当前方案的名称和 Prompt 输入框。
- 已新增 AI 分析完成后的结果摘要框，展示实际 AI 来源 / 模型、候选片段数量、标题、开始时间、结束时间和视频长度，并提供“转到片段审核”按钮。
- 远程按钮已改名为“DeepSeek AI 分析”，本地 `.env` 已更新 DeepSeek 配置并通过连通性测试。
- Docker 已改成本地开发模式：后续修改 `app/` 或 `prompts/` 后，容器会读取本机文件；普通刷新网页即可看到 HTML/CSS/JS 变化，Python 改动会自动重载。
- 下一步用真实任务分别点击“DeepSeek AI 分析”和“本地 AI 分析”验证：DeepSeek 成功时审核页应显示远程模型；如果仍显示本地，优先检查远程 Key、余额、模型权限和网络连通性。

## 2026-05-23 本次小改动：Docker 名称统一
- 已把 Docker Compose 项目名统一为 `niuma-studio`，Docker Desktop 里不再继续使用目录名 `newproject2` 作为项目名。
- 已把构建镜像固定为 `niuma-studio:latest`，以后重新构建会使用明确镜像名，减少旧 `<none>` 镜像堆积带来的困惑。
- 已确认普通 `docker compose up` 只是启动容器，不会每次复制 2GB；只有重新构建才可能留下可清理的历史镜像。

## 2026-05-23 本次小改动：AI Prompt 方案
- 已把任务详情页的 AI 分析配置从“AI 偏好”升级为“AI Prompt 方案”，支持 1、2、3 号全局共用方案、方案命名、Prompt 编辑和当前任务选择。
- 1 号方案已内置默认直播切片分析专家 Prompt；远程 AI 分析前会二次确认，避免误点后覆盖已有候选片段。
- 下一步建议补充 2、3 号 Prompt 方案，例如强观点型、知识密度型或爆点型，并用真实长视频对比候选片段质量。

## 本次小改动
- 已按要求将 DeepSeek 远程默认模型改为 `deepseek-v4-flash`，远程地址仍为 `https://api.deepseek.com`，协议仍为 `chat_completions`。

## 2026-05-29 本次小改动：封面 MVP 方案添加
- 已在发布中心接入封面 MVP：选择切片、填写标题和封面时间点后，可以生成“视频截图 + 暗色遮罩 + 标题字”的本地封面。
- 封面文件会保存到任务目录 `07_covers/`，创建发布任务时会把封面路径写入 `publish_jobs.cover_file_path`。
- 当前先不调用 AI 生图，后续可作为可选模式补充到封面工具里。
- 下一步建议用一条真实切片测试：生成字幕成片后进入发布中心，选择“带字幕成片”，生成封面，确认预览图和发布记录都正常显示。

## 2026-06-02 本次小改动：发送中心 2.0
- `/publish` 已改为发送中心：不再配置开放平台 API、Client Key、Access Token 或 OAuth 账号，改为查看待发送队列并确认发送。
- 发送队列默认从已完成切片读取“切好的原片”、候选封面帧、AI 标题、AI 话题和平台状态，并生成抖音 + B站两条平台任务。
- 封面改为从视频中选帧：页面可生成多张候选帧并手动切换，不再默认做标题大字封面。
- 自动发送依赖 Chrome 已登录状态 + opencli 网页自动化；如果遇到验证码、登录失效或平台风控，任务会失败并等待人工处理，不绕过平台限制。
- 下一步建议先打开 `http://127.0.0.1:8001/publish` 或本轮测试地址 `http://127.0.0.1:8002/publish`，点击“刷新发送队列”，确认视频、标题、话题和平台卡片正常显示。

## 2026-06-04 本次小改动：修复 opencli 检测
- 发送中心现在可以识别 Windows npm 全局安装目录里的 `opencli.cmd`，当前本机检测结果为 `C:\Users\10578\AppData\Roaming\npm\opencli.cmd`。
- 如果页面还显示“没有检测到 opencli”，优先重启项目后台服务并刷新 `/publish`；如果仍然出现，再检查服务是否运行在 Docker 容器里，因为 Docker 容器内看不到 Windows 主机的 opencli。
- 下一步建议在 Windows 本机启动项目后进入 `/publish`，确认顶部 opencli 提示消失，再勾选一条任务测试“发送此条”。

## 2026-06-04 本次小改动：修复 opencli `--window` 参数
- 已修复发送中心调用 opencli 时 `unknown option '--window'` 的报错，原因是参数位置放错。
- 下一步继续在 Windows 本机版 `http://127.0.0.1:8002/publish` 测试“发送此条”；如果页面已打开但上传 / 填写失败，再根据具体错误继续调整选择器。

## 2026-06-04 本次小改动：修复抖音 `Not allowed` 上传错误
- 已修复抖音上传视频时报 `{"code":-32000,"message":"Not allowed"}` 的问题：视频上传改为从 `http://127.0.0.1:8002/media/...` 读取本地切片后注入抖音上传控件。
- 当前本机服务必须继续使用 `8002`，或在 `.env` 设置 `OPENCLI_LOCAL_BASE_URL` 为实际本机访问地址。
- 下一步继续测试抖音“发送此条”；如果后续卡在“作品描述 / 自主声明 / 发布按钮”，按新的错误信息继续微调表单填写步骤。

## 2026-06-04 本次小改动：修复标题输入框选择器
- 已修复上传视频后填写标题时报 `selector_ambiguous` 的问题：发送中心现在不会再用容易匹配到多个元素的标题 CSS 选择器直接填写。
- 抖音和 B站标题填写都会优先选择当前页面里可见、可编辑的标题输入框。
- 怎么测试：重启本地项目服务后，打开 `/publish`，先只勾选一条待发送任务，点击“发送此条”，观察视频上传完成后标题是否能自动填入。
- 下一步：如果标题已经能填入，再继续看“简介 / 标签 / 发布按钮”是否还报新错误；如果仍失败，把新的完整报错内容复制出来继续修。

## 2026-06-04 本次排查：旧后台进程导致旧报错
- 如果仍看到 `input[placeholder*='标题'],textarea[placeholder*='标题'] matched 2 elements`，说明浏览器调用到的很可能还是旧后台服务。
- 本轮已经重启 Windows 本机 `8002` 服务，并确认 `http://127.0.0.1:8002/publish` 可以打开。
- 怎么测试：在浏览器地址栏确认是 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5` 强制刷新，再只选一条任务点击“发送此条”。
- 下一步：先不要用 `8001` Docker 页面测试 opencli 自动投稿；Docker 里通常看不到 Windows 主机的 opencli 和 Chrome 登录态。

## 2026-06-04 本次小改动：修复简介填写截断
- 已修复简介/描述填写时 `filled: true` 但 `verified: false` 的问题：抖音和 B站简介现在也使用脚本填写，不再依赖 opencli 对 `contenteditable` 的严格精确校验。
- 怎么测试：重启 `8002` 后打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5`，只选一条任务点击“发送此条”，观察标题和简介是否都能填完。
- 下一步：如果简介能过，继续看是否卡在标签、发布按钮、原创声明或平台人工确认弹窗；出现新错误就把完整错误发出来继续修。

## 2026-06-05 本次小改动：修复抖音话题、AI封面和发布按钮
- 抖音发送时，“作品描述”现在只填写发送中心的正文 / 简介；平台 #话题会单独写成抖音页面里的蓝色话题块。
- 抖音封面现在等待并选择“AI智能推荐封面”的第一个可用推荐图，不再使用发送中心封面。
- 抖音发布按钮现在精确点击底部“发布”，不会再误匹配“高清发布”。
- 怎么测试：重启 `8002` 后打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5`，只选一条抖音任务点击“发送此条”；在抖音页面检查描述是否和发送中心一致、话题是否变成蓝色块、AI 推荐封面是否被选中。
- 下一步：如果出现 `douyin_ai_cover_not_ready`，说明抖音 60 秒内没有生成可用 AI 推荐封面；如果出现验证码、风控或人工确认弹窗，先记录错误，不绕过平台限制。

## 2026-06-05 本次小改动：优化片段审核播放器和操作区
- 片段审核页右侧播放器已放大到页面内容区约三分之一，并保持在当前画面内，向下滑动候选片段列表时不用再回到页面顶部看视频。
- “保存修改”“生成切片”“去字幕推送”已改成更明显的操作按钮，“生成切片”继续作为主操作。
- 窄屏或上下堆叠布局下，点击下方候选片段的“播放预览”会自动滚到播放器位置。
- 怎么测试：重启项目后打开某个任务的 `/tasks/{task_id}/clips/review`，往下滚到第 4、第 5 条片段，点击“播放预览”，确认右侧播放器保持可见并播放对应时间段。
- 下一步：用真实任务确认播放器尺寸是否舒服；如果右侧仍觉得小，可以继续把双栏比例从约 `2:1` 调到更接近 `1.7:1`。

## 2026-06-05 本次小改动：修复抖音话题、封面和发布链路中断
- 已修复抖音发送时卡在“插入话题”步骤的 `SyntaxError: Unexpected token ')'` / `gt 不是命令`：opencli 现在不再直接走 Windows 的 `opencli.cmd` 执行复杂 JS，而是用 Node 直接运行 opencli 主入口。
- 抖音话题插入不再拼接容易被命令行拆坏的 HTML 字符串，改为在页面里创建 `data-mention="#"` 话题节点。
- AI 推荐封面选择增加文案兜底查找，会优先找“AI智能推荐封面 / 智能推荐封面 / 推荐封面”区域里的可见图片。
- 怎么测试：重启 `8002` 后打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5`，对刚才失败的抖音任务重新点“发送此条”；观察简介之后是否继续写入话题、选择 AI 推荐封面并点击底部“发布”。
- 下一步：如果这次能走到发布按钮但平台弹出验证码、风控、登录失效或人工确认，不要绕过，记录页面提示后再处理；如果仍失败，把新的完整错误发出来。

## 2026-06-05 本次小改动：先跑通抖音封面确认和发布
- 抖音话题暂时不再走蓝色话题块插入，发送时会把发送中心的 `#话题` 直接追加到作品简介里。
- AI 推荐封面点击后，如果出现“是否确认应用此封面？”弹窗，会自动点击“确定 / 确认 / 应用”，再继续点击最后的“发布”。
- 怎么测试：重启 `8002`，打开 `http://127.0.0.1:8002/publish`，按 `Ctrl + F5`，重新发送刚才那条抖音任务；重点看封面弹窗是否点了“确定”，然后页面是否继续点底部“发布”。
- 下一步：如果还卡在封面区域，把当时页面截图和发送中心的新错误发出来；如果卡在自主声明或平台风控，就先按页面要求人工确认，不做绕过。

## 当前已处理

- 已新增 Docker 一键启动方式：`docker compose up --build`，固定访问 `http://127.0.0.1:8001`。
- Docker 容器内已安装 FFmpeg / FFprobe，系统状态页可用于确认视频处理依赖是否可用。
- Docker 版继续使用项目内 `data/workflow.sqlite3` 作为数据库，并把任务产物挂载到 E 盘 `E:\直播间切片工作流存储`。
- 已修复任务详情页转写完成后循环自动刷新的问题，已完成任务页面可以正常停留和编辑 AI 偏好。
- 长音频转写已改成后台分段执行：默认每 2 分钟一段、段间重叠 5 秒，完成后自动拼接到 `transcripts/transcript.md`。
- MVP 默认转写配置已改成 `medium / cpu / int8`，先保证 Windows 本地稳定跑通；CUDA 加速后续单独验证。
- 点击“生成转写 MD”后，任务详情页会自动轮询 `/api/tasks/{task_id}/transcript-status`，不需要反复手动刷新。
- 页面会展示当前转写模型、设备、计算类型和分段进度；如果进度长时间不更新，会提示可能卡住并建议重新生成。
- 已新增转写环境检查脚本和真实短音频冒烟测试脚本。
- 任务详情顶部已改为“开始处理 / 继续处理”，会自动完成“提取音频 → 转写”；生成切片只保留在片段审核页。
- 已增加完整转写原文页面，转写预览处可以直接打开完整 `transcript.md`。
- 远程 AI 失败时会自动尝试本地 AI，并把两边错误写清楚。
- AI 配置已修正：远程 Key 兼容 `OPENAI_API_KEY`，URL 拼接增加防呆，本地默认模型改为当前机器更容易跑通的 `qwen3:8b`。
- 已新增 `.venv\Scripts\python.exe scripts\diagnose_ai_environment.py`，用于检查远程中转站和本地 Ollama 是否真的可用。
- 本地 AI 片段审核已改为分段分析，当前 `测试2 - 19min` 已用 `qwen3:8b` 生成 5 条候选片段并进入待审核。
- 片段审核页已经可以按候选片段起止时间查看对应转写原文；后续重新运行 AI 分析时，摘要、推荐理由和剪辑建议会按更详细 Prompt 生成。

## P0：下一阶段优先做

1. 打开发送中心 `/publish`，点击“刷新发送队列”，确认已完成切片会生成抖音 + B站两条待发送任务。
2. 对任意一条队列点击“选封面帧”，确认能生成候选帧、切换封面并保存。
3. 点击“重新生成话题”，确认 AI 标题、话题和简介能补齐；如果 AI 配置不可用，应能看到可读错误。
4. 先确认 Chrome 已登录抖音创作者中心和 B站创作中心，再勾选 1 条任务测试“发送此条”。
5. 如果出现验证码、登录失效或风控弹窗，把任务状态作为失败处理，并记录错误信息，不尝试绕过平台限制。
6. 再完整跑一条真实视频流程：“上传视频 → 开始处理 → AI 分析 → 人工审核 → 自动切割 → 刷新发送队列 → 发送”。
7. 继续保留转写验证：运行 `.venv\Scripts\python.exe scripts\diagnose_transcription_environment.py`，确认当前使用的是项目虚拟环境。
8. 使用真实长视频和真实转写文本验证远程 AI / 本地 AI 的候选片段质量；如果摘要仍太短，重新运行 AI 分析让新 Prompt 生效。
9. 在 Docker Desktop 中确认项目、镜像和容器名称都显示为 `niuma-studio`，并验证容器启动、停止、端口展示和日志查看体验是否足够适合新手使用。
10. 进入字幕工作台 `/subtitles/{task_id}`，对已经出现小方块字幕的切片重新点击“自动加字幕”，确认新生成的视频中文能正常显示；旧视频不会自动批量重做。
11. 打开发送中心 `/publish`，点击“刷新发送队列”或“AI 补齐标题/话题”，确认每条待发送任务自动出现封面预览，不需要先手动点“更换封面帧”。
12. 检查发送中心的“平台 #话题”字段，确认内容是类似 `#直播切片 #高光片段` 的平台话题，而不是标题解释。
13. 用包含“屎、死、诈骗、加微信”等词的测试切片标题或摘要刷新队列，确认生成后的标题、话题和简介会自动换成更温和的表达。

## P1：随后推进

1. 用 FFprobe 读取更完整的视频编码、分辨率和音频信息。
2. 为不同内容类型增加可选 Prompt 模板，并让它与任务详情里的 AI 偏好一起进入 AI 分析流程。
3. 支持 AI 分析结果人工二次生成或局部重跑。
4. 支持打开输出目录、播放输出切片和删除失败/过期输出记录。
5. 继续优化片段预览体验：支持直接预览已生成切片、显示当前播放时间和更精确的片段区间高亮。
6. 为已隐藏任务增加恢复入口，当前隐藏不会删除任何文件。
7. 根据 `docs/PROJECT_GUIDE.md` 继续补充更适合新手的一键检查脚本。

## 暂不做

- MacBook 自动录屏。
- 自动识别直播间是否开播。
- 绕过抖音 / B站验证码、登录失效或平台风控。
- AI 生图封面和复杂封面模板。
- 多用户权限系统。

# Content Profile 真实素材验收规范

## 当前验收规则（用户于 2026-09-14 明确批准更新）

工程测试、兼容检查和运行验收通过后可以发布并继续下一版本；真实内容质量在日常使用中补验。用户不再需要专门提供访谈/知识各两条素材或逐条完成三集盲审。访谈、知识模板标为“试用”，不声明真实质量已达标。原片保护、AI 不确定请求处理、人工审片、字幕与排期/发布边界均保留。

工程验收与质量结论是不同字段：旧脱敏 JSON 记录当时 `version_accepted=false`、`human_review=pending`，作为历史证据原样保留；新政策不是补填人工通过。以下真实批次、哈希和限制仍有效，但旧“人工通过前不发布/不进入下一版”的条件已由本节替代。

截至 2026-09-14：已登记三集不同康熙原片、转写及历史响应哈希，完成冻结历史响应的旧/新代码回放，各 18 单元、结果、输入/请求指纹以及渲染 Prompt/schema 哈希一致、0 次模型调用。完整证据在本机清单目录。原 Job 未冻结全部任务参数，此次使用共同读取参数、空反馈及阻断调用的 Provider 替身，不追认历史执行环境。真实分析批次与人工审片分别记录在下方；回放和合成测试不代表真实质量验收。

## 2026-09-14 技术验收记录

- 交付代码：PR #93—#97 依次合并；正式服务当前 `4eaff62`，版本号仍为 2.3.0。最终生产代码完整本地回归 1078 passed、0 failed、0 skipped，Ruff/compileall 通过；PR #97 的 Linux、Windows、Docker CI 全部通过。
- 旧库迁移：9 → 10 → 11 → 12 条迁移分 PR 验证；新增不可变 Profile/Prompt 版本与可空快照，旧任务/Run 不补猜版本。最终部署 37 表旧行原字段不变，没有新增业务数据，integrity=ok、外键异常 0。
- 浏览器：真实 Chrome 1440/390px 检查五模板创建页、旧任务、审片和复盘页；只读请求，无脚本错误与页面级横向溢出。没有用真实投稿作 smoke test。
- 冻结历史响应回放：`ffdb77f` 与 `4eaff62` 的康熙 Analyzer，每集 18 单元一致。召回阶段没有独立 request_fingerprint 参数，因此另外捕获每一单元渲染 Prompt 与输出 schema 哈希，不只比较可空字段。
- 真实模型：独立进程在 `a5ece66` 启动，原 Codex CLI / gpt-6-astra / `promptv_preset_001_003`；使用隔离库和复制的转写/音频，未重新转写、切片或写入正式候选。后续检出/部署不改变已加载进程，不能标记为最终补丁的真实调用。
- 补丁桥接回放：以本次已完成的真实响应和隔离库相同的 20 条反馈，比较 `a5ece66` 原读取与 `4eaff62` 显式反馈快照，结果及渲染请求一致。仅证明共同输入下的代码兼容，不代表人工认可或完全重建原始 Provider 请求。
- 素材保护：三集原片/转写 SHA-256 与初始清单一致，隔离转写/音频与原文件相同；音频哈希在此时补录，不假称运行前已记录。三个原片接口的 Range GET 返回 206 和正确总长度，证明入口可读；未将此当作浏览器实际播放或人工观看验收。

Prompt 正文哈希：`c27bcd8a8dd421019e1e19121eee304b70b4ca4b4c36cb12c03b047bf6181129`。Profile 配置哈希：`677b6fddda50f749128b7674cbc2c30e1002d235815231cda270639ddd6119c7`。没有修改生产 `preset_001` 正文。

| 样本 | 冻结历史回放 | 本次真实分析 | 人工质量 |
| --- | --- | --- | --- |
| kangxi-01 | 18 单元一致 | 17:34 完成，18/18、100%、12 候选 | 待审 |
| kangxi-02 | 18 单元一致 | 17:50 完成，18/18、100%、12 候选 | 待审 |
| kangxi-03 | 18 单元一致 | 18:05 完成，18/18、100%、12 候选 | 待审 |
| interview_story × 2 | 单元/集成模拟通过 | 待提供合适素材 | 待审 |
| knowledge_opinion × 2 | 单元/集成模拟通过 | 待提供合适素材 | 待审 |

三集真实 Run 的 failed_units/invalid_item_count 均为 0，analysis_incomplete/quality_degraded 均为 false，没有 heartbeat 异常或不确定单元，共 54 个成功单元、36 条候选。第三集 18:05:38 完成，独立进程正常退出。三集反馈桥接回放也全部通过；没有为此再次调用模型。状态依据本机 `real-model/run.json`、各样本 `real-analysis.json` 及 `technical-summary.json`；可提交的状态与哈希见 [脱敏证据](CONTENT_PROFILE_ACCEPTANCE_EVIDENCE.json)，人工与新类型未完成行不能推定通过。

### 工程交付及日常质量跟踪

1. 三集真实分析的技术检查已完成；真实观看质量仍未决定。
2. 本机 `人工对比材料-完整批次.md` 保留作日常检查材料，不强制用户逐条盲审。主控继续核查已有转写的上下文、边界、重复与证据；机器检查不能改写为人工认可。需要盲审时映射文件仍单独保管。
3. 访谈/知识在用户自然使用相关素材时检查各自价值与完整性；此前页面标试用。发现问题按具体素材/时间点修复复验，不要求用户先筹集专门测试集。
4. VERSION/Worker/页面统一为 2.4.0，经必要回归、CI、Windows 实机发布门禁和运行验收后交付，再进入 v2.5；实际完成情况记入 PROJECT_STATUS，不以本政策更新自动认定所有技术检查通过。

### 运行回退

本次反馈补丁没有新迁移；备份在 `data/backups/feedback-evidence-deploy-20260914-174451/`。优先暂停新分析入口并保留兼容读取代码。新 Job 已冻结反馈时，不应直接用忽略该字段的旧程序继续执行；已有 Profile/Prompt/Provider 快照和 checkpoint 必须保留。不得用旧数据库覆盖当前数据库作为无损回滚，也不得把已发生的发布事实当作可撤销的数据。

## 本地清单

在未跟踪的 `data/acceptance/content-profile-v2.4/` 保存 manifest.json、原始响应、回放结果与人工记录；媒体、私人转写、真实 Prompt 正文、完整数据库不提交 Git。
至少三集不同康熙节目，覆盖稳定高质量、跨窗互动、候选不足；新访谈/知识各另选两条合适素材。

| 字段 | 必须保存 |
| --- | --- |
| sample_id/content_type | 脱敏编号、内容类型 |
| source | 本地路径、大小、SHA-256、FFprobe 时长，原片不改 |
| transcript | 路径、字节哈希、转写器/语言/配置与时间范围 |
| strategy | 基线提交、Profile/规则版本、参数、Prompt 版本与正文哈希 |
| provider | 实际 Provider/模型/推理配置指纹，不包含 Token/Cookie |
| baseline_run | 历史 Run/Job、阶段请求/响应哈希、coverage、失败/不确定单元 |
| replay | 新提交/输入指纹/响应来源、窗口与候选边界/排序/分数/等级/默认选择 |
| blind_review | 匿名 A/B 映射独立保存、审阅人/时间、逐候选结论原因、总门禁 |

缺失来源明确为空附原因，不补猜。

## 两层验收

1. 确定性回放：相同转写、Prompt、Provider 配置、冻结响应输入旧/新路径，比较窗口、边界、排序、分数、等级、默认选择与请求指纹。抽配置本身应一致；成功单元复用，不确定不能记成功。原响应缺失时明确无法证明该样本回放等价，另补完整样本。
2. 真实质量对比：独立记录真实调用和新 Run，随机 A/B 展示原片上下文与候选，检查铺垫、核心价值/笑点、反应、收尾、断章和重复。新增断章、丢笑点闭环或明显回退即失败，修正复验；数量与 AI 自评分不代替人工结论。

人工质量待验不再阻止后续工程迭代；保留“试用/待验”事实，不宣称质量无回退。访谈/知识按自身价值与完整性检查，不套用 humor 门槛。

## 可提交证据

仅提交脱敏样本 ID、输入/结果哈希、代码提交、模拟/真实标记、差异与人工结论摘要；完整材料留本机。版本记录具体命令、通过/失败/跳过、CI、实际运行版本和未满足门禁，不用旧测试数冒充当前结果。

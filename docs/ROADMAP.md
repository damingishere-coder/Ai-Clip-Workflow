# 项目路线图

## 版本策略

- **P0**：紧急修复和安全补丁
- **P1**：基础架构补强（数据库、查询、性能）
- **P2**：工程化与架构演进准备
- **P3**：分布式与多机协作
- **P4+**：多用户与平台化

当前版本：**v2.1.0**（Windows 本地高光生产闭环增强版）

---

## 已完成

### v1.0 — MVP 基础闭环

- [x] FastAPI 项目骨架
- [x] SQLite 数据库 + 表结构
- [x] 任务 CRUD（新建/列表/详情/删除）
- [x] 视频上传与 NAS 路径选择
- [x] FFmpeg 音频提取
- [x] 任务状态流转

### v1.1 — AI 分析链路

- [x] 火山引擎远程转写
- [x] 本地 faster-whisper 转写
- [x] DeepSeek 远程 AI 分析
- [x] Ollama 本地 AI 分析
- [x] AI Provider 抽象层
- [x] 候选片段审核页（启用/禁用/编辑）
- [x] FFmpeg 自动切割
- [x] 任务目录 `task_dir_name` 管理
- [x] 回收站软删除

### v1.2 — 字幕与发布

- [x] ASS 字幕生成 + FFmpeg 合成
- [x] 字幕样式预设
- [x] 发送中心队列（抖音 + B站）
- [x] AI 文案生成（标题/简介/标签）
- [x] 候选封面帧（07_covers/）
- [x] opencli 辅助浏览器投稿
- [x] Apple 风格 UI 全面美化
- [x] 文档收拢与测试补齐

### P2-1 — 工程化

- [x] 代码检查（Ruff）
- [x] CI 流程（GitHub Actions）
- [x] Git 安全规范文档

### v1.3 ~ v1.5 — 自动流水线与真实发布架构

- [x] 全自动任务流水线、失败步骤重试和历史任务续跑
- [x] SQLite Scheduler、Publisher Registry 与北京时间 / UTC 统一
- [x] Windows Chrome Worker、抖音 Publisher 与 B站 Publisher
- [x] `PUBLISHED` / `FAILED` / `NEED_REVIEW` 保守终态和执行事件
- [x] 内容准备、排期计划、执行记录和显式 `manual_export`

### v2.0 — 分支收拢与本地生产闭环

- [x] E 盘统一生产存储、临时上传、发布包与安全永久删除
- [x] 综艺笑点优先 / 通用内容价值两种 AI 选片模式
- [x] AI 封面时间点、历史内容封面补齐与双平台复用
- [x] 片段审核保存、按需重新切片并同步发送中心
- [x] 任务详情每 3 秒局部更新进度、日志与可用操作
- [x] 发送内容按原始任务分组，已排期状态清晰可见
- [x] 跨午夜排期、续接平台最晚排期、日期详情和时间升序
- [x] 取消发送返回内容准备，旧执行证据继续保留
- [x] 项目说明、架构、流程、路线图和新手文档统一到 2.0

### v2.1 — 本地运行、AI 与审核体验增强

- [x] 固定综艺笑点优先模式，并按最近创建时间提供任务名称历史候选
- [x] 统一 Docker / Windows 原生日常启停、Worker 检测和安全回退说明
- [x] 接入受控 Codex CLI 文本分析，保留远程 DeepSeek 与本地 Ollama 兼容路径
- [x] 发送中心固定抖音前台，统一账号预检、文案规则、旧草稿升级和任务组全选
- [x] 片段审核支持当前列表全选、异步切片进度与重复切片任务拦截

---

## 进行中

### v2.1 真实环境验收

- [ ] 抖音使用单条低风险素材完成登录、上传、表单填写、封面、提交和成功证据验收
- [ ] B站使用单条低风险素材完成同等灰度验收
- [ ] 登录失效、验证码、风控和结果不确定场景都能进入 `NEED_REVIEW`
- [ ] 连续运行 Docker Watcher、Scheduler 与 Windows Worker，验证中断恢复和日志完整性
- [ ] 用真实长视频继续校准候选质量、文案、封面时间点和排期体验

---

## 计划中

### v2.x — 本地可靠性与安全增强

**目标**：保持单用户 Windows 本地架构，优先提高可靠性，不急于引入分布式组件。

| 任务 | 说明 |
| --- | --- |
| 数据备份 | 增加面向新手的一键 SQLite 备份、完整性检查和恢复说明 |
| 磁盘预警 | 新建任务前检查 E 盘剩余空间，避免长视频处理中途失败 |
| 处理并发 | 明确 FFmpeg、转写和 AI 任务的本机并发上限与排队提示 |
| 操作追踪 | 为永久删除、人工确认发布结果等高风险动作补充本地审计记录 |
| 平台维护 | 平台页面变化时更新选择器和成功证据，不绕过平台验证 |

---

### P3-1 — 数据库升级

**目标**：从 SQLite 迁移到 PostgreSQL。

| 任务 | 说明 |
| --- | --- |
| PostgreSQL  schema 设计 | 对应现有 10 张表，利用 JSONB、枚举类型 |
| 数据迁移脚本 | SQLite → PostgreSQL 一键迁移 |
| 双写过渡期 | 可选同时写 SQLite + PostgreSQL，验证数据一致性 |
| 查询优化 | 利用 PostgreSQL 索引、全文搜索 |

### P3-2 — 消息队列与 Worker 分离

**目标**：API 和 Worker 解耦，支持横向扩展。

| 任务 | 说明 |
| --- | --- |
| Redis 引入 | 缓存 + 消息队列 Broker |
| RQ / Celery Worker | 独立 Worker 进程处理耗时任务 |
| API 只做调度 | Router 层只负责创建任务、查询状态，不执行处理 |
| Worker 监控 | Worker 心跳、任务超时、死信队列 |

### P3-3 — 对象存储支持

**目标**：任务产物不限于本地磁盘。

| 任务 | 说明 |
| --- | --- |
| 存储抽象层 | `StorageBackend` 抽象基类 |
| 本地文件系统 Backend | 当前实现 |
| NAS Backend | 网络挂载目录 |
| MinIO / S3 Backend | 对象存储（自建或云服务） |

---

### P4-1 — 用户体系

**目标**：支持多用户，每人独立空间。

| 任务 | 说明 |
| --- | --- |
| 用户注册/登录 | JWT 鉴权，密码哈希存储 |
| 角色权限 | admin / operator / viewer |
| 用户隔离 | 任务数据、存储目录、AI Key 按用户隔离 |
| Profile 管理 | 用户可修改密码、昵称、默认配置 |

### P4-2 — 多租户与配额

**目标**：为团队使用做准备。

| 任务 | 说明 |
| --- | --- |
| 租户概念 | 一个租户 = 一个团队/组织 |
| 存储配额 | 每租户最大存储空间 |
| 任务配额 | 并发任务数、AI 调用次数限制 |
| 使用统计 | 仪表盘展示存储用量、任务统计 |

### P4-3 — 审计与监控

**目标**：完整的可观测性和操作追溯。

| 任务 | 说明 |
| --- | --- |
| 审计日志 | `audit_log` 表：谁、何时、做了什么、结果 |
| Prometheus 指标 | 任务成功率、API 延迟、队列长度 |
| Grafana 仪表盘 | 实时监控面板 |
| 告警规则 | 任务失败率过高、磁盘不足、Worker 离线 |

### P4-4 — 发布账号安全托管

**目标**：平台 OAuth Token 的安全存储和自动管理。

| 任务 | 说明 |
| --- | --- |
| Token 加密存储 | AES-256 加密 access_token / refresh_token |
| 自动刷新 | Token 过期前自动刷新 |
| 权限最小化 | 只申请必要 scope |
| 撤销机制 | 用户可随时撤销授权 |

---

## 暂不计划

以下事项**目前不在路线图中**，等有明确需求后再评估是否加入：

| 暂不计划 | 原因 |
| --- | --- |
| SaaS 多租户平台 | 个人工具定位，不需要计费、租户管理、SLA |
| 真实全自动一键发布 | 平台风控/验证码/登录失效，技术上不可靠 |
| 强制云部署 | Windows 本地是核心场景，不强制上云 |
| 移动端 App | 工作流依赖 FFmpeg 大文件处理 |
| 实时直播流接入 | 需要流媒体技术栈（RTMP/HLS/WebRTC） |
| 多人实时协作 | 冲突解决和锁机制复杂度高 |
| 第三方 API 直连发布 | 抖音/B站开放平台权限门槛高 |

---

## 版本号规则

- 主版本号（P 阶段）：架构大变更
- 次版本号：功能增量（如 v1.1 → v1.2）
- 修订号：Bug 修复和小改进

当前 `VERSION` 文件记录版本号，`app/main.py` 中 FastAPI `version` 参数同步。

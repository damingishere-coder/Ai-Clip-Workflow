# UI 参考说明

## 2026-05-25 更新：抖音 + B站发布中心
- 左侧导航新增“发布中心”，位置在“字幕推送”和“系统状态”之间。
- 新增 `/publish` 独立页面，页面首屏直接进入后台操作，不做营销页。
- 发布中心包含“抖音后台”和“B站后台”两个页签，每个页签都包含应用配置、账号管理、待发布切片、批量配置和发布记录。
- 待发布切片沿用 Apple 风格浅色卡片，支持选择原始切片 / 带字幕成片、发布账号、标题、简介、标签，以及平台专属字段。
- 抖音后台显示真实发布按钮，并在前端二次确认；B站后台显示开放平台配置和稿件字段，但在未拿到投稿接口权限前保持明确的待接入口径。
- 发布记录展示发布模式、账号、视频来源、状态、失败原因和重试 / 标记 / 取消操作，方便人工追踪每条切片是否已经发出。

## 2026-05-25 更新：片段审核源监视器
- 片段审核页候选片段卡片新增“编辑出入点”按钮，用于打开深色源监视器弹窗。
- 源监视器上方展示源视频，下方展示当前时间码、时间轴缩放、蓝色入点 / 出点范围、播放头和左右拖拽手柄，视觉参考剪辑软件源监视器。
- 第一版按秒级时间编辑，支持设为入点、设为出点、跳转、预览片段、1 / 5 / 15 秒微调和应用到片段；最终保存仍使用页面原有“保存修改”。
- “查看这一段转写”会使用页面上当前的起止时间，即使还没有保存到数据库，也可以先核对新范围的逐句原文。

## 2026-05-25 更新：运行日志浅色化
- 任务详情页右侧“运行日志”不再使用黑色终端样式，改为浅色蓝白玻璃卡片，和页面整体 Apple 风格保持一致。
- 日志文本使用深灰色等宽字体，背景保留轻微蓝色层次，滚动条使用蓝色细节，避免在浅色后台中显得突兀。
- 运行日志仍位于“日志 / 元信息”侧栏内，继续显示当前任务 `logs/process.log` 的最新内容。

## 2026-05-24 更新：AI 分析日志和进度条
- 任务详情页 AI 分析按钮下方新增独立进度条，用于展示保存 Prompt、启动分析、分段分析、降级本地 AI、完成或失败等状态。
- 任务详情页右侧“日志 / 元信息”侧栏新增“运行日志”框，显示当前任务 `logs/process.log` 的最新内容；最新样式已调整为浅色蓝白卡片。
- 点击 DeepSeek AI 分析或本地 AI 分析后，页面会自动刷新运行日志；用户可以直接在页面右侧观察 AI 分析进展。

## 2026-05-24 更新：国内远程转写
- 任务详情页转写进度新增实际转写来源显示，会展示火山引擎远程转写或本地 faster-whisper，以及模型、设备和音频格式。
- 任务详情页转写入口拆成“AI 转写（火山引擎）”和“本地转写”；已有转写时对应显示“重新 AI 转写”和“重新本地转写”。
- `transcript.md` 仍保持原有分钟级转写和逐句时间戳原文结构，后续 AI 分析、片段审核和字幕抽取不需要改入口。
- 当前默认转写 Provider 为火山引擎；远程失败时可按配置退回本地 faster-whisper，页面错误和日志会提示具体原因。
- 转写运行中会显示“停止转写”按钮；停止是请求式操作，会在当前分段结束后把进度标记为已停止。

## 2026-05-24 更新：v1.1 清理与数据呈现
- 左侧状态说明更新为“v1.1 本地切片全流程”，明确本地上传、转写、AI 分析、人工审核和自动切片已走通。
- 工作台统计新增“已切片任务、待加字幕、待推送”等指标，并在今日处理焦点中突出输出切片、字幕和发布前确认。
- 任务列表新增“输出切片”列，后续工作流会直接提示“待加字幕 / 待推送”。
- 字幕推送页统计文案改为来自已生成切片，不再使用 Demo 阶段口径。
- 根目录重复设计图已清理，正式参考图仍为 `docs/design/live_streaming_slicing_workflow_ui_16x9.png`。

## 2026-05-23 更新：字幕与推送工作台
- 左侧导航新增“字幕推送”，位置在“片段审核”和“系统状态”之间。
- 任务列表新增“后续工作流”列，切片输出存在时显示“待加字幕”，让字幕成为切片后的可见流程环节。
- 新增 `/subtitles` 页面：展示切片后视频队列、本地视频预览、删除 / 修改剪切 / 自动加字幕入口、字幕样式模板、打码设置和一键推送配置。
- 字幕样式当前先保存到浏览器本地，后续需要接入 SQLite 模板表和 FFmpeg 字幕渲染。

## 2026-05-23 更新：AI Prompt 方案
- 任务详情页的 AI 分析板块从单个“AI 偏好”输入框升级为 1、2、3 号 Prompt 方案卡片。
- Prompt 方案全局共用，可编辑方案名称和完整 Prompt；当前任务通过单选按钮选择其中一个方案用于远程 / 本地 AI 分析。
- 远程 AI 分析前会二次确认，提示本次会使用当前 Prompt 方案并覆盖已有 AI 候选片段结果。
- 1 号方案默认写入直播切片分析专家 Prompt，并继续支持 `{{MAX_CLIP_DURATION}}`、`{{TARGET_CLIP_COUNT}}`、`{{AI_PREFERENCE}}`、`{{TRANSCRIPT_TEXT}}` 变量。

## 1. 主要视觉参考

后续前端实现以以下设计图为主要视觉参考：

```text
docs/design/live_streaming_slicing_workflow_ui_16x9.png
```

如果页面结构与设计图存在轻微差异，页面结构以产品需求为准，视觉风格与布局优先参考设计图。

## 2. 设计原则

- Apple 风格。
- 简洁、高级、留白充足。
- 白色 / 浅灰背景。
- 蓝色作为主强调色。
- 轻量玻璃拟态感。
- 卡片式信息分组。
- 清晰、克制，不做密集企业后台风格。
- 首屏应直接进入可用后台，不做营销式落地页。

## 3. 主要页面

左侧导航当前顺序为：工作台、新建任务、任务列表、片段审核、字幕推送、发布中心、系统状态。

### 1. 工作台首页

包含左侧导航、顶部搜索、任务统计卡片、v1.1 流程展示和最近任务列表。

### 2. 任务列表

包含搜索、平台筛选、状态筛选、排序按钮、任务表格、状态标签和分页样式。

当前任务列表已增加“隐藏”操作。隐藏只从页面列表移除任务，不删除 E 盘任务目录和视频文件。任务列表会展示输出切片数量和“后续工作流”，用于提示切片完成后是否进入“待加字幕 / 待推送”。

### 3. 新建任务

包含任务名称、视频上传、平台类型、单条切片最大时长、候选片段数量和流程说明。

当前新建任务页只保留上传本机视频入口。上传视频会保存到 E 盘任务目录；NAS / 本地路径选择、浏览路径和打开目录入口已从页面移除。

### 4. 任务详情

包含任务基础信息、状态概览、AI 分析、转写预览、输出结果和日志 / 元信息侧栏。

当前标题区会展示任务 ID、写入时间、主题和当前状态，方便用户对照本地任务文件夹。状态概览已合并处理进度时间线，使用地铁路线图节点表达：绿色表示已通过，蓝色表示当前阶段，黄色表示异常或待处理问题。

当前顶部主操作为“开始处理 / 继续处理”，会自动完成“提取音频 → 生成转写 MD”；转写完成后主按钮不可重复点击，只保留明确的“重新生成转写”。“生成切片”不再放在任务详情顶部，只在片段审核页执行。远程 / 本地 AI 分析入口已移入“AI 分析”板块，用户可先填写 AI 偏好；偏好只影响候选片段推荐方向、推荐理由和排序参考，不会直接改变最终成片。转写预览只展示真实带时间戳的转写文本，并提供“查看完整原文”入口；`transcript.md` 默认可由火山引擎远程转写生成，远程失败时按配置退回本地 faster-whisper，文件仍包含分钟级原文整理和逐句时间戳原文。转写进度会自动刷新，并展示当前服务商、模型、设备和计算类型。

### 5. 片段审核

包含候选片段列表、启用开关、标题、起止时间、摘要、推荐理由、传播价值、剪辑建议、AI 来源 / 模型、真实视频预览、片段转写抽屉和生成切片操作。

当前片段审核页不再展示模拟候选片段。没有真实候选片段时显示空状态；有真实候选片段时会从 `clip_candidates` 表读取，并支持保存标题、起止时间、启用状态和摘要修改。右侧面板会读取任务源视频并提供真实播放器，预览栏宽度和视频高度已受控，竖屏视频会完整缩放显示；点击候选片段“播放预览”会跳到对应时间段播放；点击“查看这一段转写”会从右侧打开转写抽屉，显示该片段对应的逐句原文。“生成切片”按钮已接入真实 FFmpeg 切割流程，右侧面板会展示切片输出结果。

片段审核 AI 分析策略已更新：远程 DeepSeek 和本地 Ollama 都使用 3 分钟左右的小段分段分析，再合并、去重、排序候选片段，避免 40 分钟或 1 小时以上转写一次性分析导致 JSON 截断。AI 返回旧字段时会自动兼容，例如 `clip_key` 会转为 `clip_id`，缺失的传播价值会补成可审核字段。任务详情页只展示可读的失败摘要，详细校验错误保留在任务日志。

`/clips` 总览页已升级为片段审核工作台，会展示待 AI 分析、待人工审核、可生成切片、已完成和异常任务统计，并按任务提供“详情”和“进入审核”入口。单任务审核页没有候选片段时，会显示返回任务详情、生成转写和运行远程 AI 分析的下一步按钮。

### 6. 字幕推送

包含切片后视频队列、切片视频预览、字幕样式模板、打码设置和一键推送配置。

当前页面属于切片后的后续工作台：已生成切片会从 `output_clip` 表读取并显示；如果本地文件存在，可以直接播放预览。不满意时可返回片段审核页修改剪切；删除、自动加字幕、打码和推送按钮先作为后续接入入口。字幕样式支持字体、字号、位置、颜色和阴影开关，当前保存到浏览器本地。

### 7. 发布中心

包含抖音后台、B站后台、开放平台应用配置、发布账号、待发布切片、批量发布配置和发布记录。

当前发布中心已具备真实平台发布前的准备链路：抖音支持保存开放平台凭证、生成 OAuth 授权链接、选择账号并调用 provider 执行上传 / 创建视频；B站先保存开放平台配置和稿件字段，等待投稿接口权限后补齐真实 provider。页面不会保存平台账号密码，不使用 Cookie，不模拟浏览器发布；点击真实发布前会二次确认。

### 8. 系统状态

包含 E 盘存储目录、FFmpeg、FFprobe、SQLite 数据库、任务数量和最近异常任务状态。

当前系统状态页已补充推荐运行地址 `http://127.0.0.1:8001`、AI 配置状态、任务状态统计和 AI 配置弹窗。AI 配置弹窗保存到 `.env`，API Key 输入框默认脱敏，留空会保留旧密钥。弹窗头部和底部操作区固定可见，支持点击关闭、取消、遮罩区和 ESC 关闭；本地 AI 区域已简化为 Ollama 模型下拉选择。

系统状态页现在会直接展示远程 AI Base URL、协议、片段审核模型、远程 Key 是否有效，以及本地 Ollama 是否在线和已安装模型。远程 Key 仍不显示明文；如果 Key 为空或明显过短，页面会把 AI 配置标记为异常。远程 AI 默认面向 DeepSeek，推荐 `https://api.deepseek.com`、`deepseek-v4-flash` 和 `chat_completions`；本地 AI 默认推荐 `qwen3:8b`，避免 `qwen3:14b` 在当前机器上长时间超时。

## 4. 当前实现状态

2026-05-16 本轮已根据 UI 设计图升级 5 个后台页面：

- 工作台：首页已具备左侧导航、顶部搜索、统计卡片、工作流程进度线和最近任务表格。
- 任务列表：已具备搜索、平台筛选、状态筛选、排序按钮、任务表格、状态标签和分页样式。
- 新建任务：已具备任务名称、视频上传、平台类型、可填写的切片时长、候选片段数量、右侧流程说明和开始处理按钮。
- 任务详情：已具备基础信息、地铁路线式状态概览、AI 分析偏好填写、远程 / 本地 AI 分析入口、转写预览、输出结果和日志 / 元信息侧栏。
- 片段审核：已具备候选片段卡片、启用开关、标题、起止时间、摘要、推荐理由、传播价值、剪辑建议、AI 来源 / 模型、右侧真实视频播放器、片段转写抽屉和审核操作按钮；保存修改会写回数据库并显示页面级错误提示，生成切片会调用 FFmpeg 并显示输出列表。

当前任务、新建任务、路径存储、音频提取、本地真实转写 Markdown、AI 片段分析入口、系统状态、片段审核保存、源视频预览和切片生成已接入真实本地流程。v1.1 后续重点是完善字幕添加和平台一键推送。

2026-05-17 本轮补齐：任务隐藏按钮、AI 配置弹窗、片段审核工作台、片段审核空状态下一步操作，以及系统状态页运行地址提示。后续验证请优先使用 `http://127.0.0.1:8001`。

2026-05-17 本轮修复：AI 配置弹窗关闭体验和默认配置。远程 AI 默认使用 `https://ai.oneinfinityai.com`、`gpt-5.5`、Responses 协议、`xhigh` 推理强度和不保存响应；本地 AI 默认走 Ollama，并在页面中提供 `qwen3:8b`、`gemma3:12b`、`qwen3:14b` 三个模型选项。

2026-05-17 本轮调整：新建任务页只保留上传视频入口，移除 NAS / 本地路径相关控件；上传按钮改为自定义样式；切片时长改为手动填写分钟数；AI 偏好移动到任务详情的“AI 分析”板块。

2026-05-17 本轮优化：任务详情页标题区补充任务 ID、写入时间和主题；处理进度时间线并入状态概览，改为地铁路线式节点；转写预览不再展示 Markdown 元信息，只展示真实带时间戳的转写文本，暂无真实内容时给出明确提示。

2026-05-17 本轮转写：`生成转写 MD` 改为调用本地 faster-whisper，转写阶段只做语音识别和按分钟整理真实原文，不调用 AI；后续候选切片仍由 AI 分析板块负责。
2026-05-19 本轮转写体验：`生成转写 MD` 改为后台分段执行，任务详情页的转写预览区域新增进度条，展示当前分段、总分段数和百分比；长音频完成后仍在同一位置展示真实带时间戳转写文本。
2026-05-20 本轮转写体验：任务详情页会自动轮询转写状态接口，进度条会展示更细的准备、模型加载和分段转写状态；如果进度长时间不更新，会提示可能卡住。默认转写配置改为 `medium / cpu / int8`，优先保证本地 MVP 跑通。
2026-05-20 本轮工作流：任务详情顶部改为一键处理，自动衔接音频提取和转写；新增完整转写原文页；远程 AI 403 / 网络 / 权限类失败时自动降级本地 AI；详情页右侧日志栏加宽并支持长路径换行。
2026-05-20 本轮 AI 配置：系统状态页补充远程 Key 有效性、本地 Ollama 在线状态和已安装模型展示；AI 配置弹窗提示兼容 `OPENAI_API_KEY`，本地模型默认推荐 `qwen3:8b`。
2026-05-20 本轮片段审核：本地 AI 分析改为分段分析与合并候选，`测试2 - 19min` 已用本地 `qwen3:8b` 生成 5 条候选片段。
2026-05-23 本轮 AI 分析 UI：任务详情页的 AI Prompt 方案改为文件夹标签式 1、2、3 号切换，下方只展示当前方案的名称和可拉长 Prompt 输入框；AI 分析完成后在同一区域展示实际 AI 来源 / 模型、候选片段摘要和“转到片段审核”按钮，远程失败自动降级本地时会直接提示降级原因。远程分析入口文案改为“DeepSeek AI 分析”，避免用户误解当前接入的远程模型来源。
2026-05-23 本轮 Docker 开发体验：Docker Compose 挂载本机 `app/` 和 `prompts/`，并开启 `uvicorn --reload`；页面静态资源关闭浏览器缓存，后续前端 UI 调整后刷新页面即可看到最新效果。
2026-05-23 本轮 AI 历史结果：任务详情页 AI 分析区域新增候选片段数量输入框、刷新后保留的分析预览，以及“历史分析结果”面板；历史面板按第 N 次分析展示来源、模型、时间、候选条数和总结，并提供恢复整次分析结果的按钮。

## 2026-05-24 更新：自动加字幕页面结构
- `/subtitles` 从单一大工作台改为“字幕任务列表”一级页面，只展示已经生成切片的任务，避免不同视频混在一起处理。
- 新增 `/subtitles/{task_id}` 单任务字幕工作台：左侧展示该任务的每条切片、原视频预览、字幕状态、带字幕成片预览和操作按钮；右侧保留字幕样式、打码和推送配置。
- “自动加字幕”按钮已接入真实处理状态，完成后在同一卡片内展示带字幕视频预览。
- “修改剪切”改为弹窗式预览，弹窗包含视频播放器、类似手机剪切的时间轴滑块和“去片段审核保存”入口。
- 视觉仍沿用 Apple 风格、轻量玻璃拟态、蓝色主强调色和 8px 卡片圆角；字幕任务列表使用和片段审核总览一致的任务行布局。
 
## 2026-05-25 更新：字幕工作台剪切弹窗
- `/subtitles/{task_id}` 的“修改剪切”弹窗已按手机剪辑截图方向升级：源视频在上方，下方是黑色帧条、左侧播放按钮、粉色外框、黄色选区和左右入点 / 出点手柄。
- 弹窗会围绕当前片段自动放大时间轴，让长视频里的短片段也能明显显示黄色选区，方便拖拽。
- 用户可以拖动左右黄色手柄，也可以输入入点 / 出点时间，或把当前播放位置设置为入点 / 出点。
- 点击“保存入点 / 出点”会直接写回候选片段数据；但输出切片文件不会自动替换，仍需要回片段审核页重新生成切片。
